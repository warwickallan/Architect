# 25, 30, 35. Other topic nodes
# Udemy — Phillip Burton — AB-100 — lecture 014 (section 2: Creating Copilot Studio classic orchestrated single-agent)
# https://www.udemy.com/course/ab-100-agentic-ai-business-solutions-architect-exam-preparation/
# captions: English

[00:00] In this video we're going to have a look at some other nodes that you can add in. So first of all there is ask with adaptive card and we've seen references to adaptive card when we were adding messages. So there we can see an adaptive card and it does the same sort of thing. So if I delete that message and add in an adaptive card and to design it I would click on it and then click on edit adaptive card. So here is my design and I can add things such as a text block so I'll drag that into there. So for this text block on the right hand side I will say how many vacation hours do you want to book and then I'm going to add an input number. So I'll click on it first and then drag. So for the label I'll say please enter it below. For the placeholder so that's the input area we'll say number of days and if I want it to be stored in a variable I would add an id. So this is the variable name so I'll say number days. Now for an adaptive card to work you will need to add in an action set and add an action and the action needs to be a submit. So this is an ok or send button. So let's say submit or send. Now I can preview this and you can see how it looks. It is adaptive because it changes what it looks like based on the width that you've got. So if I click close, save and close you can see that there are two outputs from this adaptive card. The action submit id which we don't need and the variable called number days which we can use later on if we so wish. So variable management is the next section so we can set a variable value.

[02:09] So this is useful if you want to create a new variable. So I will click on select a variable, create a new variable, click on the variable and now I can say what the variable name is going to be. So let's say number of hours. The type is currently unknown and we have the same options that we have seen before. So now I can set it to a value. So if I set it to the number zero you can see that the type now changes to a number. If I say that the value is none and press tab then it changes to a string.

[02:51] There are other options within variable management including list all variables. In topic management you can go to another topic, you can end the current topic, you can end all topics and end the conversation. You can also transfer the conversation or escalate it to a human. So ending conversation is useful when you want the topic to end and for there not to be any new topics. Without an end conversation node it may show this message and then go on to another topic. So I'll click on end conversation and add that.

[03:35] We can also add tools. So these include basic tools, connectors to other systems and we can also add a tool. We'll be looking at tools in a later section and we can also add additional agents as well and there's additional options in the advanced section as well. But for now I'm going to delete all of these new nodes apart from the end conversation and then I will click save. So in this video we've had a look at some other types of nodes that you can use in a topic. So you can ask with adaptive card so that gives you so this can be useful if you want to ask for more than one piece of information at once all in the same card and you can also manage variables, topics, add tools and agents and there's also some advanced options as well.
