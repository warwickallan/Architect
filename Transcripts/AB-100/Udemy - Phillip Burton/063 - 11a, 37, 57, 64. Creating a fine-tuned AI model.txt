# 11a, 37, 57, 64. Creating a fine-tuned AI model
# Udemy — Phillip Burton — AB-100 — lecture 063 (section 10: Agents and models in Microsoft Foundry)
# https://www.udemy.com/course/ab-100-agentic-ai-business-solutions-architect-exam-preparation/
# captions: English

[00:00] In the previous video we saw that there were over 11,000 models available for use in Microsoft Foundry. However that might not be sufficient for you. You might want your own custom AI model to be created. So what you can do in Microsoft Foundry if I go to build and models is that you can deploy a base method or a fine-tuned model. So this is a model which is based on a base model and then adapted based on your data.

[00:34] This is called fine-tuning. So this is useful when you have a little data to retrain the model. So why is this important? Well you can reduce prompt engineering overhead because with a fine-tuned model you don't need to add a few-shot learning into the prompt as it already has it as part of its training. It's also useful when you want to modify the style or tone. You want to create outputs in particular formats or schema. You have examples of responses which have used particular tools or knowledge sources.

[01:09] You're transferring knowledge from a larger model to a smaller model thereby improving latency and cost. Now the pre-trained model is trained to do one task but you might want it to do a different task. For example you might want a fine-tuned model to be trained for a particular skill or domain such as in the medical, legal or for a particular educational department. Now you don't just have to use a base model you can use a model that you have already trained to train a more specialized and smaller model.

[01:40] This is called distillation. This can improve cost and latency as it is a smaller model. Now do bear in mind that not all base models support distillation. When should you not use fine-tuning? Well if you've got a lot of data to train your model then training your model from scratch may be better. And another alternative is to give the engine the information needed as part of the prompt or in the chain of conversation. How is a custom AI model actually trained? Well it uses something called low-ran adaption which is capital L small o capital R capital A. This reduces the fine-tuned model's complexity without significantly affecting its performance. So what are some of the advantages of fine-tuning models? Well they include better results and prompt engineering. You can train on more examples than you can do with a prompt because you have a context limit for the amount of information you can use in a conversation.

[02:43] Shorter prompts allow for saving in the number of tokens used. You don't need to provide as many examples in your prompts. And you'll also have lower latency requests so quicker responses especially when using smaller models. So let's create a fine-tuned model. So if I click on fine-tune and then one of the fine-tune buttons I can give it some basic details. So I can select the base model. Now you can click on show all to see more base models. The customization method and there are three of them. Supervised fine-tuning or SFT. This is when you give the model examples of prompts and responses.

[03:31] This is the usual method. Direct preference optimization or DPO. This is when the model learns from comparative feedback. It needs examples of preferred outputs and non-preferred outputs. And then there is one customized model which is for the 04 mini model only. This is reinforced fine-tuning. This is best for domains where there are clearly right and wrong answers and where guessing is not effective like math, chemistry and physics. Next we have the training type. So this could be standard. So this is done in the same region.

[04:10] Global. This can be cheaper but you will need to check with your compliance policies to see if this is allowed as the processing can be done in other regions. And then there is developer. This is for non-urgent workloads. It may be cheaper but may take longer as it doesn't have the same priority to resources. So to take an example of the difference of the costs. For this model the training is about $120 per hour for regional or standard training. $100 an hour for global and $50 an hour for the developer training.

[04:54] Then we have the training data. So we can use an existing data set or upload a new one. We can use sample data sets pointed to somewhere on the internet and also have stored completions. So this is the conversation history from chat completion sessions. So for this example I'm going to use a curated sample data set and I will use this text gsm8k train sample as an example. Now what does that contain?

[05:27] Well here we can see an example. So first of all it is in jsonl format. So we have got messages which contains a role. The role is either system or user and following that we have the content. And at the end we have another role which is the assistant and again we have content. There may be more than one user assistant per conversation.

[05:58] So let's have a look at the details of that data set. So here we have got messages role system content reply users question as accurately as possible. Role user content and here's a question. When you see this figure then that is new line and then role assistant content and then a response. And you can see it is a carefully calculated response showing all of the working. Also notice that there is a new line at the end, the answer and then three exclamation marks. Going further down I can add validation data. So this allows me to test the new finely tuned model and see whether it is successfully done. So there is no option this time for a curated sample data set. However I'm going to upload a new data set and click upload data set and here you can see my JSON L file. So it's just uploading and you can see it is in the same format as the training data.

[07:09] You've also got the opportunity to add a suffix. So this is good for the ALM process, the application lifecycle management process because you can add suffixes to various fine-tuned models and therefore you can go back a stage to the previous suffix if your latest version is not optimal. So I'm going to leave everything else as is and click submit. So you can see that my fine-tuned model job has been sent for submission and is now queued. Well this might take several hours so I'm going to pause the video until the model has been created. Well it's taken a couple of hours but now you can see the status is completed. So if I click on the model you can see that there are two key metrics.

[07:58] The final train loss, this shows how wrong yet confident the model was when predicting the next correct token during testing. It's a number between zero and one. The lower the better. And there's final train mean token accuracy. This is how well the model did and again it's a number between zero and one. This time the higher the better and you can click on view all metrics to see how this score changed over time.

[08:29] So if I smooth it out a bit here we can see the change. So now that our fine-tuned model has been created let's deploy it. So I'll click on deploy. We will be looking at all of these options in a later video and then click deploy. Now it has been deployed I can test it by going to the playground. Now this will take a few minutes for the model deployment to be ready so I'll pause the video. So that took a few minutes but now it is ready. So I'm asking it a math question so let's submit that and see what answer we get. And you can see here is our answer including having the final figure with three exclamation marks at the end. So in this video we saw how we can create a fine-tuned model. So you go to build fine-tune click on fine-tune again and then you can select the base model the customization method the training type and some training data and optionally some validation data.
