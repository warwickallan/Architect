# 9, 73a. Using generative AI and knowledge sources in Copilot Studio agents
# Udemy — Phillip Burton — AB-100 — lecture 021 (section 3: Creating Copilot generative AI single-agents)
# https://www.udemy.com/course/ab-100-agentic-ai-business-solutions-architect-exam-preparation/
# captions: English

[00:00] In this video, we're going to look at the use of generative AI and knowledge sources in Agents built with CoPilot Studio. Well, we've previously seen that there are two different types of orchestration, which you can change in the settings. Either classic orchestration, which doesn't directly use generative AI, or using the generative AI orchestration, responses will be dynamic. However, that doesn't mean that classic orchestrated agents cannot use generative AI. As we have seen when we go to Topics and System, the conversational boosting topic does use generative AI. So here you can see it, and you can add it to any topic by going to Advanced, Generative Answers.

[00:47] If I go back to the overview and scroll down to the knowledge section, you can see that you can also enable or disable the web search. So enable your agents to search public websites. If you want your agents to focus on the knowledge sources, then you might want to disable web search. So what knowledge sources can you use? Well, if I click on Add Knowledge, you can see that knowledge sources include public websites. So for agents which have been built using the classic orchestration, you can have up to four public URLs.

[01:22] For agents which have been built using the generative AI orchestration mode, you can have up to 25 different websites. So I could put in, for example, https://www.microsoft.com, and I can add this. So you should enter a description so that the computer knows when it should search this website. Websites can be up to two levels deep.

[01:51] So you could have Microsoft.com, Microsoft.com forward slash something, and Microsoft.com forward slash something forward slash something else. The websites cannot require authentication and will need to be indexed by Bing. As these websites can be changed because they are not under your control, this is a way for prompt manipulation to happen. Instructions could be added into those data sites to try to override what your agent is meant to do. It is advised not to use social networks, search engines, or forums as knowledge sources.

[02:26] So I can click add to agent. So there we can see our new knowledge source. Now you'll notice that there is a see all link here. If I click this, then this goes to the knowledge tab at the top. And we can see more information about these knowledge sources. So we can see what type it is, for example. Now it could be that you are adding public websites over which you have no control over. Now suppose there is a particular website that you know is going to be definitive.

[02:58] Well, if I click on the dot dot dot, I can go to official source, and I can change that from no to yes. So that means that the computer doesn't need to verify the information which is contained in that website. So only do this for websites that you can trust. And you can see if you do that, then it will update your agent instruction. So I'm not going to proceed with that, and I'm going to delete this knowledge source. So if I go back to add knowledge, we can also add in documents.

[03:34] You can add a lot of documents if you wish to do so. You are only limited to the amount of the file storage allocation in your Dataverse. The Dataverse is the database that is built into the Power Platform of which CoPilot Studio is part of. So you can either upload it from your own website, or local storage, or from OneDrive or SharePoint. And it can be up to 512 megabytes per file. Another knowledge source is SharePoint.

[04:05] So you can have up to 4 URLs for classic orchestration agents, and up to 25 URLs for generative AI orchestrated agents. For unstructured data, for example, Word documents and PowerPoint presentations, you can have up to 500 knowledge objects. So we're talking files, folders, knowledge articles, websites, or other sources. Now when you add a SharePoint source, then it will need to say ready. Now it may initially say ready briefly when first being added.

[04:39] It will probably then change to in progress, and it cannot be used until it says ready again. Looking at other different knowledge sources, you've got Azure AI Search. So that is used to index data. Now while services such as SharePoint and OneDrive use Enter ID, there may be different authentication requirements for services such as Azure AI Search. For example, if I create a new connection, you can see that there are four different types of authentication, all of which require an endpoint URL and more.

[05:15] So there's access key, client certificate authentication, service principle using Microsoft Enter ID, and Microsoft Enter ID integrated. Now, regardless of which you use, you should ensure that you have sufficient access controls on the relevant permissions and that they are protected. You've got the Dataverse. Again, that's the database behind the Power Platform. So for the classic mode, you've got up to two Dataverse sources and up to 15 tables per knowledge source.

[05:47] In the generative mode, there are no such limits. So I could add, for example, the address table. Now services like SharePoint, Dataverse, and Dynamics 365 use Microsoft Enter ID. This manages authentication, identity, who you are, and authorization, access, what data should you be able to access. It is necessary that users only have access to the data that they should be able to have access to.

[06:18] When you add services such as SharePoint sites and Dataverse tables, these are then retrieved into Dataverse and indexed for use. The reason for indexing is because querying the source directly for data each time has a high cost and could be complex. Now, it may take up to around 5 to 30 minutes for Dataverse to be configured for the first time and then to index the data source. We've also got Dynamics 365, Salesforce, ServiceNow, and Azure SQL Database as knowledge sources.

[06:55] For databases like Azure SQL Databases, you may wish to use a read-only endpoint. An endpoint is the place that one service can talk to another. So with databases like this, you can create additional endpoints. So the standard endpoint is a read-write endpoint. So that is where services can write to a database. Adding an additional read endpoint means that services such as Co-Pilot Studio Agents can read data from the database and not degrade the performance from any services which are trying to use the standard endpoint.

[07:31] And if I click on Advanced, then you can have Snowflake, Databricks, Confluence, Oracle Database, SAP or Data, Zendeck, Azure Databricks, and Bing Custom Search. And there may be more by the time that you see this. Now we've got all of these different types of knowledge sources like Dataverse and Azure SQL Database. You are restricted to a maximum of five different types of knowledge sources.

[08:02] So SharePoint is one source. Dataverse is another source. It doesn't matter how many tables you add from Dataverse. It's just that Dataverse counts once for the maximum of five different types of knowledge sources. While you can add files which have sensitivity labels of Confidential or Highly Confidential, it will not use them. And for OneDrive and SharePoint, you can have up to 1000 files, up to 512 megabytes per file, up to 50 folders, and up to 10 layers of subfolders.

[08:35] It will use the files of DOC, DOCEX, so the Word documents, XLS and XLSX, so Excel documents, PPT and PPTX, PowerPoint documents, and PDFs. You cannot upload image, video, executables, or audio files. You can, however, upload images which are embedded into PDF documents. So in this video, we've had a look at the different knowledge sources that you can use in Agents built with CoPilot Studio.

[09:04] And we've also had a look at how you can add Generative AI to Agents which use Classic Orchestration.
