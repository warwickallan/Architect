# 6. Child (inline) agents versus connected agents
# Udemy — Phillip Burton — AB-100 — lecture 039 (section 7: Designing multi-agent solutions in Copilot Studio)
# https://www.udemy.com/course/ab-100-agentic-ai-business-solutions-architect-exam-preparation/
# captions: English

[00:00] In this video we will be looking at the difference between inline or child agents and connected agents. So at the moment we have been using single agent solutions and they are best for narrow and well-defined problems managed by a single agent, centralized reasoning and a single context. So the advantages are simplicity, they're easier to design, create, develop and maintain, lower overhead, increased predictability and easier debugging. However the problems with single agent solutions include harder to scale and to extend, a single point of failure and it can't assign tasks everywhere, it does everything all itself. So this is when multi-agent solutions come in. They can use many agents which can work independently, they could even be designed by other people than yourself and they can communicate to each other using messaging or shared context.

[01:01] Now if I go to the agents tab and it's also down here in the overview tab, I can add an additional agent and generally speaking there are two different kinds. Inline agents which are also called child agents and connected agents. So let's talk about inline or child agents. These are topics inside the main agent with specialized tasks. The parents tools and knowledge can be made available to the child agent. The child agents share the parents topics. You'll notice that there is no way of adding a topic to this child agent. Child agents do not need to be published separately so it is all one big agent in terms of publishing. You can set the priority for this child agent.

[01:53] So this is useful if an agent and a topic both respond to the same event which is called first. The lower number would be called first. So if I put in the number one then it's almost certain to go first. If I put in the number one thousand or ten thousand then it's less likely. So you can add a trigger. So the triggers are similar to what we've seen previously. So the agent choosers being the default trigger. You can set instructions, knowledge and tools. You can set enabled or unset enabled if you temporarily don't want to use it. And you can add inputs. So if I was to add an input then you can specify for example the data type. And in the advanced section of the inputs section you can say if an input is not provided then you can set should prompt user.

[02:51] The inputs can be validated using conditions and you can also add a condition not met prompt and also an action if no entity is found. And if the input is not valid then you can say how many re-prompts. So how many times can it be asked. Again up to two times. And you've also can customize the retry prompt. So these are inline or child agents. So now if I go back to the main agents tab I can delete that. I can also enable or disable the inline or child agent as well.

[03:34] So in addition to child agents you can also have connected agents. So these are separate published agents. They can have different knowledge, tools, privileges and topics. There should be a clear path as to when to call the connected agent and this path should be stated in the parents in the main agents configuration. The data that will be handed to the connected agent should be clearly defined. You should make sure that security is maintained. For example if it's intended that the parent or main agent cannot delete files then there might be a problem in the overall design scope of the agent if the connected agent can delete files. Make sure that everything is logged audited and monitored.

[04:20] The agents and any updates to connected agents must be published before they can be used by other agents. And in Copilot Studio you can't have an agent that connects to an agent that connects to an agent. So only one connection is allowed but you can have multiple agents within the one main agent. It's just that those agents that you connect to cannot connect to other agents. So you can see I can connect to any connected agent. If they're not published then I can't connect to them. I can also connect to external agents in Microsoft Fabric, Foundry, 365 Agents SDK, Software Development Kit or using Agent to Agent. And if you find that you cannot connect to that agent first of all make sure it's published and secondly if you go to that agent and click on settings then in Generative AI we've got a connected agent section. Let other agents connect to and use this one and make sure it is on. So when would you use a child agent and when would you use a connected agent? Well you would use a connected agent when the task is complex, when it needs its own tools or knowledge, when there are different governance or ALM application life cycle management rules or when they're maintained by different people or teams and when you can or will reuse the connected agent with other main agents perhaps in different solutions. So when to use a child agent? Well basically when none of what I've just said applies.

[06:06] Child agents avoid the additional complexity and latency in connecting to other agents. Other reasons to use a child agent? Well you might want to organize an agent into sub-agents. You don't need different authentication, deployment or publishing schedules and they don't need to be reused in other agents or solutions. So you might want to start with an inline agent and then move to a connected agent when necessary. And for both child and connected agents you can choose to send it the conversation history. You can turn this off if it is not needed for security to prevent data leakage or to improve performance. So in this video we've had a look at the difference between child agents and connected agents. In the next video we're going to see the various ways in which agents can be connected to each other.
