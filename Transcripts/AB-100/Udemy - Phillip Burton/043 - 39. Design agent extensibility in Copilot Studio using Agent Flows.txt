# 39. Design agent extensibility in Copilot Studio using Agent Flows
# Udemy — Phillip Burton — AB-100 — lecture 043 (section 7: Designing multi-agent solutions in Copilot Studio)
# https://www.udemy.com/course/ab-100-agentic-ai-business-solutions-architect-exam-preparation/
# captions: English

[00:00] In this video we're going to have a look at agent extensibility in Copilot Studio. So we've seen that our request for leave to be denied, if it's between the 12th and the 24th of December and the number of vacation days is less than two, hasn't been successful. So what can I do to make it more successful? Well I could put all of this into a tool. So if I go into tools and click add a tool, we have previously looked at custom prompts.

[00:33] So I could put that section into a prompt and then maybe use a different model and then I can try and just put this one topic in isolation. Now speaking of topics what I could also do is create a topic, an evaluation topic. So it could say if this happens then do this. Now that is a much more structured way to ensure we get repeatability and predictability of particular flows. Once that has been created then I would go back into the overview tab and if I click on edit, then under the validation leave requests I would be referencing if I put a slash that particular topic. So go through this topic and get whether it is to be accepted or denied.

[01:29] Now another way to add predictability is a different type of tool. So we had an unpredictable outcome for our adding a row to a spreadsheet. We don't have the leave type. So what I can do is add a tool and this would be an agent flow tool. An agent flow is predictable. It runs the same way each time giving you more control when you need it. So if I click on that you can see we have the agent flow designer and indeed we have an entire section on the left-hand side called flows. So at the moment we've got a trigger when an agent calls the flow and respond to the agent. So let's add a new action. So I'll click on the plus and I can search for an action. Now if you've used power automate flows this is very similar. So what you need to do is connect to any of the applications that you want to use and you can see that we've got hundreds of connectors including excel online for business, Microsoft dataverse, Microsoft forms, teams, Microsoft network weather, mail, office 365 outlook or outlook.com or office 365 users, one drive for business, one note for business, power bi, sharepoint, sql server and much more.

[02:58] If I go to the top then you can also see you can use AI capabilities to run a prompt, respond to the agent. In Microsoft copilot studio you can execute or evaluate agents. We've got human in the loop so this is a request for information or the ability to run a multi-stage approval process and you can process documents using AI builder and process text using AI builder. We'll have a look at AI builder much later in this course. So if I scroll down to Microsoft excel online I can add a row into a table. So if I click on this we'll have the same sort of information required. So first of all the location, so one drive for business, the document library, one drive, the file, again I'll click browse and I'll scroll down to leave requests and the table, this will be table one. Now notice advanced parameters, it did say briefly showing zero of one, now it says showing zero of six. If I click on show all you can see that we've got spaces for name, date from, date to, leave type and vacation days. Where can I get that information from? I can get it from the agent. So if I click on this trigger I can add inputs. So I can add name, date from, date to and so on. So if I click on text, so this creates a text variable, so I will have name for example and I can add in a description, employee name, add in a date, so date from, date to, leave type, that's text and a number, vacation days. So now if I go down into add a row into a table action, for name I can press a slash and insert dynamic content and I can enter name. You can also do this by clicking on the lightning bolt. So I can go back to my when an agent calls the flow trigger and enter date from and you can see that the description is also shown. So it would be helpful if I put in a proper description.

[05:44] So now I've got the name, date from, date to, leave type and vacation days. Now you might say that this does no more than what we previously had with our tool. However if I scroll back up to our trigger and click on the dot dot dot next to a variable you'll notice that we've got an option to make the field optional. So at the moment all of these fields are re quired. Therefore they will not be blank and I can also add a multi-selection list of options and a drop-down list of options. So I can say that the names need to be these particular names.

[06:27] Alternatively what I could do is add a condition. So if I add a new action and go into control and condition then I can say if and I'll click on the lightning if name is not equal to and click on the fx for a formula and I'll use the word null which means blank. So if the name is not blank and if all of the others are not blank then I would add add a row into the table action into the true section and exclude it from the false. So in other words if name is equal to null then it would go into the false section and if I had add a row into a table only in the true section then it would not be run. And finally when I go down to respond to the agent I can add an output. So I could add for example a variable into each of these. So I'll click on the plus and type in the word variable and I can initialize a variable saying that has a row been inserted is equal to true and in the false section again initialize the same variable has a row been inserted is equal to false and then I can return add an output a boolean output so that is a yes no output has a row been written and the answer would be based on the variable has a row been inserted. You'll notice that there were two have a row been inserted so what I would do in that case is actually initialize the variable outside of this condition and then I would set it inside this variable to a particular value. So there is the set variable.

[08:40] Now there's a lot you can do with agent flows and you could spend hours on just this one topic to have a look at all of the intricacies. However the basic things that you need to know are that agent flaws run predictably. It starts with a trigger which you can customize by adding inputs. You can then add actions such as conditions or connecting to a particular data source and then you can respond to the agent with outputs. And finally if this seems fairly complicated and you just want an initial draft then you can use co-pilot to design a flow. It can also be used to expand an existing flow. Just click on co-pilot which may be at the top or on the right hand side and then ask it what you want to do with this agent flow. So what I'm going to do right now is click on save draft. I don't want to publish it because it is not complete.

[09:42] You can also go to the overview tab to add details and you can also see the activity that has all of its previous runnings and also any analytics. So what I'm going to do now is go back into my agent go to the tools section and I can see it's not been added. So I can click on add a tool, add a tool and when it's been published I can go to flow, add the tool and then go to overview and insert references to that tool in the instructions.
