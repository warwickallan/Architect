# 26. Design autonomous agents: what are triggers?
# Udemy — Phillip Burton — AB-100 — lecture 029 (section 5: Creating autonomous agents with tools)
# https://www.udemy.com/course/ab-100-agentic-ai-business-solutions-architect-exam-preparation/
# captions: English

[00:00] Hello and in this section we will be designing autonomous agents and prompt actions together with other related things such as tools. So in a previous section we created this employee leave allowance checker. So this uses a knowledge source of leave allowance and is able to say what an employee's vacation or holiday leave or sick leave is. So for example what is the holiday leave for Philip and I should get an answer of my vacation or holiday leave and there you can see 30 vacation days. Now this sort of prompt starts by me asking a question but what if it was started by something else? So these are autonomous agents. They don't start with a user prompt but they listen for a trigger. So what I'm going to do is create a similar sort of agent to this which looks up somebody's leave but instead of it being triggered by user prompt it's going to be triggered by an email. So I'm going to edit this name so this is agent B so we've had agent A and B and then I can create a new agent C.

[01:28] So let's go back into agents and I'll start describing it. So let's say I want to create an agent which starts when it receives an email with the word vacation in the subject. It should then work out how much allowance for leave an employee has which has been named in the email. Leave allowance could be either vacation or sick leave. I will upload a spreadsheet which shows the allowance per employee for both vacation holiday and sick leave but then we've got to communicate the answer. So I want an email to be sent with details of the email received together with the leave allowance for the employee mentioned.

[02:42] So let's create that. So this will just take a few seconds and it will create the name, a description and a set of instructions. So here we can see the name we just need to wait a few seconds before I can edit it. So let's have a look at these instructions. So trigger on email, extract the employee name, look up the leave allowance. It's going to be using an Excel online business connector.

[03:13] Compose a response email and send an email. So now the agent has been provisioned let's go up and edit the name so it is agent C, vacation leave checker. Now as before I'm going to add in my knowledge exactly the same as last time so I will upload it from my hard drive and it is leave allowance and click open and add to agent. Now if you don't have that workbook you'll find it in section one of this course attached to a lecture called resources. So now I can edit these instructions. So at the moment we've got use this connect to access the uploaded spreadsheet so I'll just say use the leave allowance spreadsheet and find the rows plural matching the employee name. Once data is retrieved prepare the response email. So let's save that. Now instead of a user prompt starting this agent it's going to be started by an external event and this is called a trigger. So if I scroll down you can see here some suggested triggers and we're just going to be looking at triggers for the rest of this video. So if I click on add trigger here we can see some featured triggers. So first of all we've got reoccurrence so to run after every few seconds, minutes, hours, days or weeks. Now if an agent activates too frequently then you might exceed service load quoter limits and your performance could be throttled. Now this can be monitored in the Power Platform admin center. So you can see the default is every 30 minutes run this agent. So let's cancel this and go back into add trigger. So other featured triggers include when a new response is submitted in Microsoft Forms you need to select which form. When an item or file or Eva is created or is created or modified in SharePoint you would need to select the site address and the library and if it is a file you need to select the folder or if it is items you need to select the list. When a file is created in OneDrive for business you can also use OneDrive personal as well you would need to select a folder and optionally a subfolder. When a row is added, modified or deleted in Microsoft Dataverse you would need to select the table name and a scope together with columns. You could filter what rows you were looking at, who to run as and more. When a new email arrives in Office 365 Outlook you need to select a folder together with an optional to, cc, from, subject importance and whether attachments should be included. When a new channel message is added to Microsoft Teams so you need to select the channel among other things and when a task is completed in Planner you would need to select a group and a plan. Now these aren't the only triggers if I go to all you can see over 800 triggers. These include an RSS feed, Gmail, Power BI, Microsoft Teams, Google Calendar, OneNote and SQL Server among many many more. So what should you think about when designing autonomous agents? Well you should design its purpose, the narrower the better. You should ensure that it does not go beyond that scope into unpredictable behavior. You should give it high quality data and instructions. You should test it well and distribute it to a small number of people first. If what it can do is important then make sure that humans are able to check what it is doing. Do a first draft which works and then expand it bit by bit. Give it the minimum access it needs. Make sure that any incoming input is verified so it doesn't react to spoofed triggers.

[07:29] Incorporate limits on what it can do. For example only move files after a certain knowledge source has been checked. And ensure that it's actions are audited and that someone is reviewing those audit logs and monitoring its behavior. So in this video we've had a look at the various kinds of triggers that we can add to Copilot Studio Agents. In the next video we're going to add a trigger to make our agent autonomous.
