# 39, 40. Design agent extensibility with Model Context Protocol in Copilot Studio
# Udemy — Phillip Burton — AB-100 — lecture 044 (section 7: Designing multi-agent solutions in Copilot Studio)
# https://www.udemy.com/course/ab-100-agentic-ai-business-solutions-architect-exam-preparation/
# captions: English

[00:00] In this video we're going to have a look at a different type of agent extendibility in CoPilot Studio. So if I go into tools and add a tool you can see for example we've got all of these connectors. You can also connect to existing prompts and pre-built flows REST APIs. If I click on see all you can also create a REST API. This is an application programming interface so you could connect for example to a Microsoft Azure function and you can create a custom connector in the PowerApps portal. In this video I want to have a look at MCP model context protocol.

[00:43] So this is another way for connecting to other services including Microsoft 365. It is a recommended way to surface data and actions to agents with enterprise-grade security, authentication, and auditing. You need an agent which uses generative orchestration in order to use MCP model context protocol. So if I click on MCP you can connect to a new model context protocol server. You would need the server name, the description, the URL, the website, and the type of authentication either non-API key or OAuth version 2. If I go back and click on model context protocol here you can see some built-in MCP servers including Dataverse, Dynamics 365, Fabric, Office 365 Outlook, Kusto Query, GitHub, LearnDocs MCP, Box.com, or Jenny Actions. There are also various Microsoft MCP servers so you'll see WorkIQ. So this is Microsoft's it's a bit more than a branding but let's say a branding for certain MCP servers. So what I'm going to do is click on the email management MCP server as an example. So I've connected I can click on add and configure. So you might be asking okay what's the difference between a tool which I've previously created and an MCP server. Well it's not that the MCP server gives you a tool it gives you potentially a lot of tools. So here you can see we've got send email, reply to email, get email, list email, flag email, and forward email. By default all tools are enabled and if the MCP server creator adds any additional tools then if all tools are available then that new tool will automatically be added to the range of tools this agent can use. So I'm not just adding a single tool to an agent I am adding a system of tools. Now it could be that some tools you don't want.

[03:05] For example suppose there was a delete email and you wanted under no circumstances for your agent to be able to delete emails. Well what I would do is switch allow all to be off and then go down to delete email let's say this is it and turn it off. You might also want to turn off any other tools that the agent does not need. Remember the agent will be looking through this list of tools and selecting what it thinks it needs. If you know that there is a tool that it doesn't need you should turn it off. Now depending on the server you may also need to add inputs to configure the connection to the MCP server. In details additional details you can set whether the end user should be asked before any of these tools are used and whose credentials to use. The maker provided credentials so if this agent is shared then others will be able to use this tool based on my credentials the original author's credentials. Alternatively end user credentials are the credentials of whoever is running this agent. So once you've configured it then you can click save and you can enable these tools for use by this agent. But for this instance I'm not going to save the changes I'm not going to be integrating the email management MCP server into this agent so I'll click on the dot dot dot next to it and go to delete. So in this video we've had a look at a different type of tool model context protocol. So this allows me to import a range of tools as defined by the MCP server into my agent. Now you should note that there is a similar open source standard by Linux Foundation and defined by Microsoft Cloud called agent to agent or A2A. It is used for agents collaborating whereas MCP is for adding tools for a main agent to use. If you want to use an agent to agent then you wouldn't add it through tools, this is where you add the MCP instead. You would instead go to agents, add, connect to an external agent and go to agent to agent.
