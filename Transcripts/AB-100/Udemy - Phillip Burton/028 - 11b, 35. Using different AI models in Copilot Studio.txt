# 11b, 35. Using different AI models in Copilot Studio
# Udemy — Phillip Burton — AB-100 — lecture 028 (section 4: Grounding, Data and AI model integration)
# https://www.udemy.com/course/ab-100-agentic-ai-business-solutions-architect-exam-preparation/
# captions: English

[00:00] In this video we're going to have a look at using different AI models in our CoPilot Studio Agent. Now you can see you can select your agent's model. Your agent will primarily use the model for reasoning and responding and it used to be that there was an option for adding your own custom model which seems to have been removed somewhere around mid 2025. Now you can still use custom AI models if you've got it already created. Already created in Microsoft Foundry.

[00:33] So I can scroll down and add a tool or I can add a tool in the tools tab. Now tools are features that you can use in largely external services that will allow the agent to follow its instructions. So if I add a new tool I can add a prompt and I can write a prompt and get a response, but equally importantly I can change the model. So we have some of the models we've previously seen and if I scroll down here you can see the Microsoft Foundry models. That's the new name for Azure AI Foundry. So if I click on the plus then I'll need some details such as the model deployment name, the base model name, the Azure model endpoint URL and the API key. You might also want to add the model description. So we'll have a look at this later on in this course when we have a look at Microsoft Foundry and creating custom models. We can also use custom models in Copilot Studio in a topic. So if I go to a topic and add a node then again we can add a tool, new prompt and we can add a Microsoft Foundry model.

[01:53] This is how you can use custom models in Copilot Studio, but what about just changing the agent's model? So you can see on screen the agent's models that I've currently got. Now do bear in mind that this is a period of very rapid change and the numbers that you'll probably see will be different to the numbers that I will probably see. There are several broad categories of models, for example deep, auto and general. We'll start with deep. Deep is optimized for multi-step reasoning with tool supported flows. These are advanced AI models. They are best used for complex multi-system steps and summarization of long documents with citations. They have a high latency and a high cost. Now you can see that I currently don't have access to any deep models. You might need to enable them. So if I go into settings, generative AI, I can enable deep reasoning and you'll notice that agents do not use deep reasoning for all tasks. You should use the special word reason in the agent instructions to specify that a particular step should be used with deep reasoning. So I'm going to click save and then close this settings window.

[03:18] And if I go into the agent's model I may have access to deep reasoning. At the moment I don't though. Now please note that your data may be transferred or stored to the United States or other countries in which Microsoft or its sub processors operate. And you can see whether deep reasoning has been used for any of your prompts in the test agent by having a look at the activity map on the left hand side. Next we've got auto. Auto is optimized for mixed workloads. This is best for help desk and employee agents and queries with unpredictable complexity. And then we've got general. General is optimized for speed and cost. It is best for everyday chat and light grounding including drafting, rewriting, summarizing, translating, frequently asked questions, FAQs using grounded answers and simple action automation. General models have the lowest latency and cost.

[04:22] Now in addition to these categories there are also different types of models. So these include preview. These are not recommended for production use. They may eventually become generally available. Experimental. This is the version before preview. Now both experimental and preview models might have variability in performance, quality of response, latency, message consumption and availability. They also might not be available in all regions. Now it may be necessary for you to enable the use of experimental and preview models. If this is the case go to the wheel at the top right hand corner. Go back into the power platform admin center and then go to manage environments.

[05:07] Select your environment and then at the top we've got settings and then features which is within product and if I scroll down we should be able to enable preview and experimental AI models in copilot studio and I'm going to leave this window open because I will be looking at anthropic models later on in this video. So other types of models include default. This is the best performing generally available model. So generally available available for everybody's use though it might not be available in all regions. There's then retired. This is a default model which has been superseded by another default model. Now you may be able to use it for up to 30 days after retirement following which your agents will then use the current default model. So if you want to use the current default model when it gets retired for up to 30 days then you should go into settings generative AI and enable continue using retired models. There may also be cross geo models. So this is where data may be processed and stored outside of your company's geographical boundaries.

[06:29] Again be careful when using it if you've got policies or regulatory requirements which means that your data needs to be stored in a particular region or geographical area. Now you may also be able to choose anthropic models such as Claude and also XAI models. However you can see they are disabled by my admin. In reality they haven't been enabled. So to enable them I need to go to the Microsoft 365 admin center admin.cloud.microsoft and then on the left hand side I would go into copilot settings and then in the data access tab I would go to AI providers operating as Microsoft sub processors and here you can see anthropic is one of those sub processors and is disabled by default. So I could enable it here in Microsoft 365 admin center and then go back into the power platform admin center and enable it for use in copilot studio and the power platform. So in this video we've had a look at how we can change a copilot studio agents model.

[07:46] We've had a look at all of the different types and categories and we've also seen how we can use a custom AI as a tool. So we can add it into the custom prompt tool.
