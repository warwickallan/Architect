# 48. Propose Microsoft Power Platform AI features in the AI hub
# Udemy — Phillip Burton — AB-100 — lecture 094 (section 15: Create agents which use the Power Platform)
# https://www.udemy.com/course/ab-100-agentic-ai-business-solutions-architect-exam-preparation/
# captions: English

[00:00] In the previous video we had a look at how to extract information from business cards. In this video we're going to have a look at other AI Hub features and as a reminder you can get here by going to AI Hub and AI Models and then clicking on All. So we've got pre-built models such as extract information from invoices, extract all the text from photos, extract information from receipts, identity documents and so on. Now some of these use the AI components that we had a look at in the previous video. All of the others do not. There is a different process we need to use.

[00:41] So let's have a look at these models. So we can extract information from invoices, identity documents, contracts and health insurance cards. We can create a sentiment analysis so detect positive, negative or neutral sentiment in text data. We can classify data into predetermined categories. So here we've got categories of complement, customer service and staff.

[01:12] We can extract entities so these are key elements from text and classify them into predefined categories. So we've got here organization and date time as categories. Here we've got temperature, percentage and city. We can extract the key words from text. For example we've got Contoso account payment and here we've got Nambia and inspection. We can detect the predominant language of a text document. So here we've got English, here we've got Spanish and we can also generate a description of an image. So we've got the description a man running on a road with tags building, jogging, grass, city, sky and exercise. So let's have a look at extract information from invoices. So we can extract customer address, recipient and so on. So I'm going to use this pre-built model in an app. So if I click on this then a new canvas app will be created which will just take a few seconds.

[02:23] Now unlike the previous app that was created, this one is more of a finished app. Now in the data section you can see that it has added the AI model invoice processing. So if I play this and click on tap or click to add a picture, then here we can see a sample invoice and again you can download it from section one. It is attached to the resources lecture and go to open. Then you can see it is now processing and we will have the extracted information, the total ID and date.

[03:03] And there it is $90.20 invoice number 1783 with an invoice date of October the 23rd 2016. So if I close this let's see how this works. So if I click on this you can see that it is based on prediction result dot fields dot invoice total dot value dot text. So you can change the field to any other field which is there. Prediction result, well that is a variable.

[03:37] If I go into variables on the left hand side you can see that we've got the global variable prediction result which is set up when I change this media. So it sets the variable prediction result to invoice processing. So that is the name of our AI model. Invoice processing dot predict based on the image and then we get that prediction and extract the relevant fields. So this is how you can use the remainder of the AI models. So if I close down this page and go back into the AI hub in addition to having pre-built models we've also got custom models.

[04:29] Now these require training in other words they require your own documents. So first of all we've got extract custom information from your own documents. You'll need at least five documents for training this model but you can see the outcome. I say I want this information like for example the delivery order number give it the document say this is where they are and you can get those responses. So here is a different document and it is extracting the SOW number for example because the model was trained to extract the scope of work model. There's also classify texts so we've currently got as a pre-built model classify customer feedback into predefined categories but you can use custom categories. So if I click on this you can see an example of a custom category being urgent, needs assistance, usability, refund and bug. So with this your training data needs to be in a dataverse table with one column storing the text each of which can be up to 5,000 columns and one column storing the tags separated by comma semicolon or tabs. So if I go back into the AI model and all and classify text into custom categories these are the categories also known as tags and this is the text. You will need to supply a minimum of 10 text samples per tag so the AI model knows what text you should tag as urgent. You can have up to 200 different tags. The next custom model is extract custom entities so an entity could be a brand, CPU, RAM, screen size, GPU, hard drive, whatever you define. You will need at least 10 examples of your text data. You would need to tag individual words or phrases into an entity and you would need at least five examples per entity.

[06:44] Another custom model is predicting future outcomes based on historical data so this could be a binary prediction yes no true false multiple outcome predictions like for instance your bus will be early on time late or canceled or a specific customer will be interested in these products or it could be a numerical prediction a duration quantity or likelihood and the final custom model is detecting custom objects in images so here you can see this model has been trained to find out where green tea cinnamon is as opposed to mint as opposed to rose and it's got the probability chance that it has correctly tagged it. You will need at least 15 images per object for training so 15 green tea roses 15 green tea mints and 15 green tea cinnamons.

[07:41] You should have a balanced training data set don't have 300 images of one object and 30 for another. You should have at the most a one to two ratio between the minimum and maximum images so in other words if you've only got 15 green tea rows then you shouldn't have more than 30 mints or cinnamons. So in this video we've had a look at AI models which are contained within the AI hub and you can use these models either the pre-built models or the custom models that you have to train with canvas power apps and power automate flows.
