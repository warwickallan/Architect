# 31. Recommend cloud flow triggers
# Udemy — AB-410: Intelligent Applications Builder Associate exam prep — AB-410 — lecture 040 (section 8: Creating an instant (button) cloud flow)
# https://www.udemy.com/course-dashboard-redirect/?course_id=4049210
# captions: English

[00:00] In this video we're going to have a look at cloud flow triggers and some of the actions you can use in a Power Automate flow. So first of all you should make sure you are in the same environment that you are using in Power Apps. So I'm going to select the correct environment. Now I'm going to go to create and there are several different types of flows. Now these are called cloud flows because they run on the internet. This is to distinguish them from desktop flows.

[00:31] There is a app called Microsoft Power Automate Desktop which you may already have if you've got a Windows 11 machine. So the different types of triggers, so in other words what starts the flow from running, are automated cloud flows. These are triggered by an event, for example a new email coming in, a post on X or Facebook, a request for leave. These would be called approval flows and we'll have a look at approval flows later on.

[01:02] You've also got scheduled flows. So repeat every few minutes, hours, days or weeks. They run at a certain time and they reoccur. And then we've got instant cloud flows. These start with a tap of a button. So they can be run when a virtual button is pressed on the Power Automate mobile app. They can also be run when a physical button is clicked with third-party options or when a button is pressed inside of power apps, generally inside a canvas app.

[01:33] So instant cloud flows allow you to run a floor on demand and they may include tokens. So things like the location, the date, time and the user. So you might be able to pass into your flow the user's full address, street, city, state, postal code or zip code, country or region, latitude or longitude, the current time stamp and date or when the button was pressed and the username and user email. You can also ask for text input as well.

[02:03] So you can ask for some additional input. So let's create our first cloud flow and it's going to be a very simple flow. We're going to get the weather and then send myself a mobile notification as to what the weather is. So because I want to do it at the press of a button, this would be an instant or button cloud flow. So I'm going to call this the weather flow. So next we have to decide what the trigger is going to be. So here you can see some of the options, but I'm going to use the most common trigger, which is manually trigger a flow and click create.

[02:41] So it is now creating the basic flow. So you can see it is called weather flow and we've got this trigger called manually trigger a flow. So we can add input parameters. So suppose I wanted some information from the end user. So let's add an input text parameter and it's going to be called location. And the question is, where do you want, do you want the weather forecast for? So that is going to be a text parameter that we can use later.

[03:18] So next I'm going to add an action. So if I click on this plus sign here, we can see all of the different actions and you can see there are an awful lot of connectors. We'll have a look at how we can use connectors and actions in the next few videos. So in this video, we've had a look at the various triggers and actions that you can have in a Power Automate flow. In the next video, we will continue creating our weather flow.
