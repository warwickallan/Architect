# 15. Configure security
# Udemy — AB-410: Intelligent Applications Builder Associate exam prep — AB-410 — lecture 036 (section 7: Configure row summaries and security, and Implement business and process logic)
# https://www.udemy.com/course-dashboard-redirect/?course_id=4049210
# captions: English

[00:00] In this video we're going to look how we can configure security on tables. So access to tables is configured through security roles and we've had a look in a previous video at some of the security roles like system administrator, system customizer, basic user and delegate. Now you can create your own custom security role. So we've seen one way of doing this if we share an app then you can click on somebody and then click on manage security roles. So that will take us to this page. Alternatively you can go to the wheel icon at the top right, admin center and then go to manage environment, the relevant environment and then on the right hand side we've got access and here you can see security roles. So I can click on see all and here I can see the roles and I can create a new role if I wish to do so. So this uses the more modern interface whereas the previous one uses the classic interface but they will give you the same information. So I'm going to stick with the more modern interface. So I'm going to say that this role is the expenses role, select a business unit, I just have the one business unit, enter a description, able to view expenses, applies to, so these are descriptions of where it applies to, so expenses table, summary of core table privileges and I will just copy and paste this, able to view expenses and you can also choose to include app opener privileges for running Microsoft driven apps. So I will click save.

[02:03] So this now creates the security role. Now this security role has got very few permissions at the moment. So if I go down to the custom tables and I can search for a particular custom table like the expense table, here you can see this role has no privileges but I can click on it and I can assign privileges. So here are the privileges. So we have got create, read, write, delete, append, attach an activity or note to a record, append to, be attached a record to an activity or note, assign, give ownership of a role to another and share, give access of a role to another. So for this particular role I'm going to give read access but then the question is how much access? So we've got user access, so these are access to your own roles, roles that are shared with you and roles in which you are a team member.

[03:05] There's business unit, so they are the user roles, your own roles, plus access to information throughout the business unit. So this is usually reserved for managers, so you would have users in individual business units, it could be like a department for example. We've got parent child business units, so these are the current business unit plus all subordinate business units. So for example suppose you have got a department for human resources and within that a department for payroll. So business unit, if I'm in the HR business unit then that would give me access to all of my roles plus all of the roles in the human resources business unit but it wouldn't give me all of the roles in the payroll department even if the payroll department is a child of human resources.

[03:56] So for this I would need parent child business units and finally there is global or organization, all records for this organization. So I will just say user for example if I just want this expenses role to be able to read their own records and nobody else's. Now this is using the compact grid view which you can switch on or off. So I'm going to save this now. So these security roles can then be assigned to users, owner teams and azure active directory group teams. An owner team and an azure active directory group team can own records and can have security roles assigned to the team.

[04:39] So they would have full access rights to their records and you may see two different group types security and office. So if you want to configure an owner team then let's go back to the power apps portal and instead of going to the admin center we'll go to advanced settings and then we can go to system security and teams. We can then create a new team so if I do that we would then enter the team name, the description, the business unit, so this could be the root business unit the first one created for an organization, administrator, the team type and any other fields.

[05:24] You can also add users to an environment so if I go back to this screen then we can click on users. So this will just take a few seconds. We can then click on an existing user and we can change the business unit, we can change the teams and we can manage roles for that user. Now we can't actually add or delete users from here for that we'd need to go into the Microsoft 365 admin center. So when creating model driven apps you need to add users to a security role that is assigned to the app. So now you can create security roles, teams and users. So the security roles gives us the permissions and then you can assign the permissions to among other things teams and users. Now suppose you've given somebody through a security role access to the expenses table or the employees table but you don't want them to have access to all of the columns. For example you might not want them to have access to the salary column. So how can you do this? Well if I go into tables and employee and go to forms then you can go to form settings and say that only certain security roles can access a particular form. So you could have one form that has the salary information and other forms which do not. But people might still be able to go in into the dataverse and access a particular column. This is when column level security can be useful. So if I go into my salary column and click on advanced options and scroll down here you can see I can enable column security. So let's click on save. So who now would have access to this salary column?

[07:23] Well we need to configure a column security profile. So let's now go back into the power platform admin center into my environment and then click on settings and then under user and permissions we go to column security profiles. So if I create a new profile, so this is my salary security profile and then click on that.

[07:55] Here we can see that my salary column can be included in a security profile. At the moment read is not allowed, read unmasked is not allowed. So a mask would be like stars or asterisks. Update is not allowed, create is not allowed. So if I click on edit then I can change read to allow for example an update to allow and create to allow as well. So then I can click save and I can then add users into this column security profile. But for now I'm just going to go back into this table and turn off enable column security. So in this video we've had a look at various ways in which we can set up security. So we can create new roles and add permissions to that role. We can then assign that security role to users and teams and we can also add column level security so that not everybody who has access to the table can see the contents of that column.
