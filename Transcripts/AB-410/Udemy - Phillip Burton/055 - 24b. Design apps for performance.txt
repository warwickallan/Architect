# 24b. Design apps for performance
# Udemy — AB-410: Intelligent Applications Builder Associate exam prep — AB-410 — lecture 055 (section 12: Canvas apps: designing apps and creating reuseable components)
# https://www.udemy.com/course-dashboard-redirect/?course_id=4049210
# captions: English

[00:00] In this video we're going to have a look at the solution checker so we can see any problems with performance. So if I go to solutions and check my solution I can go to the top and click on solution checker run. So this will take a few minutes so I can't view the results yet until it is finished. So the solution checker looks at Dataverse plugins and custom workflow activities to see if they select all columns using Query APIs application programming interfaces. As we've previously said when we were looking at Power Automate getting all columns from a table is not ideal. Whether there are duplicate Dataverse plugin registrations and whether you are using batch request types in Dataverse plugins and workflow activities. We're not using plugins and workflows in this particular course. Plugins extend the functionality of the Dataverse and custom workflows do actions but if you have them then the solution checker will look at them. It will also look at HTML and JavaScript web resources so these are two of the major languages of web pages. To check that they don't target absolute Dataverse endpoint URLs the problem with this is that the endpoint might change. Don't use modal dialog boxes so modal dialogs will not allow you to click on anything else until you say okay or dismiss the modal alert and don't use certain functions like eval or alert they're a bit too general. It also looks at Dataverse configurations such as message steps so again this is to do with programming and extending the Dataverse but more to our point it will look at canvas app formula issues, delayed load so stopping the app from instantly being responsive and whether there are too many app controls. You can't use the solution checker to check the default solution so you can see that it is still running you can cancel it if you want while it runs and when it is complete we will receive an email and a notification so I'm going to pause the video until it's complete. So it took quite a while and in fact failed the first time I received an email saying that it could not be completed so then I restarted it again. So now it has been completed I can go to solution checker in solutions and view results and here we would see any of the problems and how to resolve and you can see we've got columns such as severity, issue, category, object name, type and reference and you can download the results by going to solution checker download result and it downloads as a zip file. So in this video we've seen how we can run the solution checker we go to solution we check a solution and then go to solution checker run
