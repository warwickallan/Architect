# 27a. Manage context variables
# Udemy — AB-410: Intelligent Applications Builder Associate exam prep — AB-410 — lecture 063 (section 13: Create canvas apps)
# https://www.udemy.com/course-dashboard-redirect/?course_id=4049210
# captions: English

[00:00] In the previous video we set a global variable which is available to all the screens in my app. In this variable we're going to be looking at a context variable which is only available to one screen. Now let's go to the expenses edit screen. Now let's say I wanted a variable to determine what was in the header. So what I can do on this screen is if I go to the form and just resize it I can insert a text input and also a text label. So I will say header text and insert a button set header text. Now in the on select I could use set. So set would create a global variable but I only need this variable for this one screen. So what I can do is use a context variable. So I do this using update context and then in curly brackets because I need to pass a record and the curly brackets indicates it is a record. I can say that the header text is going to be colon and then this text input. So I'll just put the number one for now and then we'll go to this text input. So this is called text header text and so I will assign header text the value of text header text dot text.

[01:50] So now I will say that this text is going to be the header text. So now if I go back to this title screen and play start the app edit an expense. So now I can click on set header text and there we've got the words text input. So I can change this to editing an existing expense. It probably won't fit that into there. Oh it will. So just by clicking on set header text it uses update context to update this context variable. Now you might be asking well why don't I just say that this is equal to this text. Well one of the things you can do with variables is not just define it in one place you can define it in multiple places. So if I go back to the title screen we are using a set to set a global variable to this value. However there might be another button that sets it to another value. So where I'm using it, so here, doesn't have to now be reliant on just button referencing one label. It could be set in multiple places. Now another way that you can set context variables is by using navigate. So at the moment we see navigate with two arguments. The first is where we want to go and the second is the transition. But there is a third optional argument and that is what do I want to set the context variables on the screen that we're going to to be. So I could say that the header text is going to be saying editing existing expense. So make sure you've got the curly brackets because we are passing again a record. And similarly if I go back to the add a new item I can say navigate to the expenses edit screen using this screen transition and set the header text on the screen that we're going to to be add new expense.

[04:08] So now let's have a look at our app. So I can change the color so I can say back to green. Just because that this is not yellow it will use green by default. So I'll click start the app. I will add a new expense and there we can see it says add new expense because that has been passed through using the navigate function. Similarly I can click on an expense and go to edit and now it says editing existing expense. And I can also change my header text to say hello there and click set header text and that will change the context variable as well.

[04:49] So when should you use global variables and context variables? Well you should use global variables where it is going to be used in more than one screen. And you can see that we use the set function to set a single global variable. If you want to set more than one then you would need one or two semicolons and then say set the next variable. Use context variables if it is restricted to one particular screen. So this improves usability because I can then use that screen elsewhere and because the context variable is restricted to just that one screen then these two variables will not interact with each other. You should also know that when you use the update context function to update a context variable you can update more than one variable at once. So I could say comma and then set another variable within the same update context function. So these are variables. You can use them when for example you have a running total or when you're retrieving the results of a dialog box or when you want to store something that might slow the computer down by retrieving the same data multiple times. Though you can also use named formulas for that. So global variables are assigned using the set function and context variables are assigned using the update context function and you can see all of the variables that you are using in your app by going on the left hand side to variables.
