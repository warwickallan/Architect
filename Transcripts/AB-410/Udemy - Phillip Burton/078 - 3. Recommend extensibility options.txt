# 3. Recommend extensibility options
# Udemy — AB-410: Intelligent Applications Builder Associate exam prep — AB-410 — lecture 078 (section 15: Design Microsoft Power Platform solutions by using AI-enabled tools)
# https://www.udemy.com/course-dashboard-redirect/?course_id=4049210
# captions: English

[00:00] In this video we're going to have a look at various extensibility options in Power Platform. So, so far in this course we've seen what you can do with creating canvas apps, model-driven apps, and tables with columns and rows in the dataverse. We've also seen how you can use Power Automate so you can create workflows that can automate processes. You can use triggers which run on changes in data or specific events, and actions in sending emails, creating rows, or calling external services to automate processes. You can also integrate services such as Azure Functions which allows you to create custom code in C Sharp, JavaScript, and Python.

[00:44] In the dataverse you can write business rules to implement and enforce business logic at the data layer. We've also had a look at how you can create business process flows to guide users through a series of steps when entering data. So how else can you expand the Power Platform? Well you can use modern commanding to extend your model-driven apps. So this will add new items to your commands at the top. So I can click on the dot dot dot next to a view and go to edit command bar. So if I want to edit the main grid then here you can see all of the commands and then I can add new commands as well. We can add additional functions and we can run JavaScript or Power Fx. So Power Fx as a reminder allows you to use Excel-like formulas allowing for a low-code environment. So you can use this in canvas apps and also in modern commanding among other places. You can create custom connectors.

[01:51] So this allows you to talk to external data sources and services. For example Azure Functions. It uses the REST API application programming interface. You can build custom user interface components using the Power Apps component framework. This uses languages like C Sharp to create your custom code and then you can package the component into a solution file. You can build plugins which can customize Dataverse data processing. You would write custom business logic which can run based on Dataverse events, technically known as messages, happening either before the operation is validated so it can be easily cancelled at this stage before the operation occurs or after the operation occurs.

[02:40] If you go into a Dataverse form you can go into the properties pane and the event tab. You can load events into the onload and onsave form so when a form is loaded or saved. The tab tab state change when a tab is expanded or collapsed. The field on change when data in a field changes and the control loses focus or for iframes, so this is external web data, on ready state complete event when the iframe content loads. It can then execute a function which is called in a javascript web resource.

[03:24] So in this video we've had a look at various extensibility options. So in this course we have had a look at power automate flows, business rules, business expenses and there are additional options such as modern engineering, power fx and more.
