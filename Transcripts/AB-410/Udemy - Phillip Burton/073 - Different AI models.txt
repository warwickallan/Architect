# Different AI models
# Udemy — AB-410: Intelligent Applications Builder Associate exam prep — AB-410 — lecture 073 (section 14: Create prompts and models in AI Hub)
# https://www.udemy.com/course-dashboard-redirect/?course_id=4049210
# captions: English

[00:00] In this video we're going to have a look at the different AI models which are available in the AI Hub. So if I click on AI Hub and AI Models and then click on All, here you can see the different types of models. Now there's two different sorts of models, custom models that you need to train and pre-built models that require no training. So let's start by looking at the AI models which can be used both as a pre-trained and a custom model. So we've got classification. So this categorizes text by meaning. So here we can see for product details visit this website. So that is a category of documentation. So I want to refinance my car loan this year to reduce cost is price and billing and you can use these languages. Now if you want your own categorizations then you can use the classified text into custom categories. So here you can see I cannot connect to your service anymore, how do I reset my password has been categorized using these categories into urgent, need assistance and usability. Next there is entity extraction.

[01:18] So this looks for things like locations, dates, emails, names and numbers. So here we've got an organization and a date. Here we've got a temperature and a time and again these can be used in these languages. But if these categories are not sufficient and if I click on them all you can see that these are the supported entity types. Now entity is also the old name for a table but in this case it means an element of text with a meaning. Now if these categories are not sufficient then you can extract custom entities from your own text using your own categories and this of course would require training. Now let's have a look at the other custom models.

[02:08] So there is a document processing model. This was previously known as form processing. It extracts text from images. It works best with text embedded pdfs. Then there is the object detection model. So this identifies objects from uploaded images. It counts the number of objects included and there are three different types of domains. We've got objects on retail shelves, so products densely packed. We've got brand logos and we've got common objects which is everything else.

[02:45] So I don't want this model so I will delete it. And we've also got a prediction model. Will something happen based on past historic data? So you can use a binary prediction, yes or no, a multiple outcome prediction where there's more than two outcomes, or a numerical prediction. Some pre-built models include a business card reader. This uses the document processing model. It's in english only. It requires an uploaded photo or picture, so jpeg, png, bitmap or tiff.

[03:23] If it's a business card then it will extract the relevant information. There is an id reader. ID reader. So this extracts information from passports and us driver licenses, social security cards and green cards. It doesn't retain the image. There is the invoice and receipt processing models. These are two separate models. The invoice processing model uses english language invoices up to a free or legal paper size.

[03:58] And then the receipt processor again uses english language receipts such as from restaurants, gas or petrol or retail. So in other words not full page invoices. You can have up to 200 pages in a pdf. The key phrase extraction model gets trends from for example x, surveys, emails and forms. You can use this in conjunction with sentiment analysis which we'll have a look at in a minute.

[04:29] So here you can see that it thinks the key text is this atm number, the word nambia and inspection. The language detection model could be used for example in a power automate flow when you want to send received emails to language specialists or if you want to save information in language specific folders. So it could output en if it is english generally, en-us if it is us english. It could also have unknown. Sentiment analysis detects positive, negative, neutral or mixed sentiment in text data. So you can use this for social media, customer feedback and identifying email sentiment. There's the text recognition model. So this is for OCRing optical character recognition both printed and handwritten text from images.

[05:32] So you can use it for car license plates, id cards and handwritten notes. And there are many other different types of models. So in this video we've had a look at the various types of AI models which are available in the AI Hub. In the next video we'll see how we can use them in canvas power apps.
