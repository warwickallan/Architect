# 7. Create and modify standard tables
# Udemy — AB-410: Intelligent Applications Builder Associate exam prep — AB-410 — lecture 014 (section 3: Creating Expenses tables in the Dataverse)
# https://www.udemy.com/course-dashboard-redirect/?course_id=4049210
# captions: English

[00:00] Now an alternative to creating tables and columns in copilot is to use the more standard more traditional method of creating tables and we do this by going to tables new table and table and in brackets advanced properties so on the right hand side we can see the new table that we want to create so i'm going to create an employee table so again we've got the display name employee and the plural name employees there will be a primary column now this should be the thing which is used to identify this row now notice that i don't say uniquely identify the row if i was talking a primary key in say sql server then a primary key column would need to be unique so we would have number one number two number three and so on for an auto number column or we would have colors like blue red green we couldn't have blue red green green however a primary column in the dataverse the values can repeat so i could have employee name as a primary column and then you could have two john smiths it is the name john smith that is used as the visualization for what links two or more tables together so choose wisely it cannot be altered after you set it up so you might want to have an employee number for example and then have one column for first or given name another column for last or family name and so on but i'm going to have in this case a primary column with a display name of employee name now if i go to the advanced options you can see here we've got the internal name the schema name whether it is required or whether it is just recommended or optional so if it's required then you cannot save any data which doesn't have this primary column it can't be blank and the maximum character count so in this case 850 characters now there are more columns that will be created in addition to the primary column there is a hidden primary key a unique identifier or a GUID g u i d globally unique identification now if i go back into the properties we can also add a description and in advanced options we can change the internal schema name the type so we're going to concentrate in this course on standard tables activity tables are for tasks they have columns such as time dimensions stop start due date duration subject description and status opened cancelled and completed activity tables include appointment and reoccurring appointment email fax letter and phone call and task a generic activity representing work that is needed to be done but in this course we're going to be concentrating on the standard tables record ownership again you could have rows owned by a user or a team this includes business units which own teams or at the organization level record ownership cannot be changed after the table has been created if you want to change it you need to delete the table and create a new table you can also choose a table image and a color and there's all sorts of other things you can do as well but for now i'm going to save this table and you can see that there is a problem in that there already is the employee table which hasn't yet finished deleting so i will call this the employee table and the plural name employees table and click save and now our table has been created with no sample data this time so in this video we've seen how we can create a standard table so we go to tables on the left hand side and go to new table table and in brackets advanced properties and then define the table and its primary column
