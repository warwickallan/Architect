# 37. Design choices and app performance, and App Checker results
# Udemy — AB-410: Intelligent Applications Builder Associate exam prep — AB-410 — lecture 143 (section 26: Other controls and other canvas apps topics)
# https://www.udemy.com/course-dashboard-redirect/?course_id=4049210
# captions: English

[00:00] In this video, we're going to have a look at how we can interpret the app checker results. And we describe the implications of design choices on app performance. And you can see how the two are connected when I click on the app checker, which is up here, it's this stethoscope. So we've seen problems in the past. We've seen where I've got formulas that are not complete or where there's an error that we had X next to the relevant control and we had a dot here.

[00:32] So the red dot on the app checker shows formula errors. But that's not all that the app checker does. First of all, there are three different types of warning signs. There are errors, so that's the red X circle, there are warnings, that's a yellow exclamation triangle. And there are suggestions, and that's a blue letter i, lowercase letter i circle. Now, you should note that the app checker is for Canvas apps only.

[00:59] When we have a look at model-driven apps, we'll be looking at a solution checker, we'll be able to check them there. So let's have a look at what we got in the app checker. First of all, we've got formulas. We've seen a few times problems with formulas. We've got accessibility. We'll be looking a fair bit later at accessibility. So it's just trying to make your app usable for people with certain requirements. And then we've got performance. So you can see, for instance, inefficient delay loading and unused media files, which means, of course, it's got to load the media file before the app could be used.

[01:37] So what are the implications of design choice on app performance? Well, maybe we've got a really big app. Well, if you've got big apps, especially if you've got big images, that can cause problems with performance and load times. So can you break that down into smaller apps? And if you're loading graphics, if you're loading videos, if you're loading video, can they be optimised for the size of the screen? You don't need something in 1920 by 1080 when it's going to be half size on a phone screen, for instance.

[02:11] Now, one of the big problems could be network bandwidth latency issues. So that can be a big bottleneck, retrieving the data. It's all due to network traffic. So don't refresh data when you don't need to, for instance. We've been using the gallery control. The gallery control automatically refreshes. You don't need to. You should store data in the fastest place possible. So we'll be looking in the next section about collections.

[02:39] So maybe you can collect a lot of data at the beginning of an app and then just refer to the collection rather than having to go through the network to continually get the same information. Maybe you're loading too much data. You're loading unnecessary properties. Could you load a screen's data only when needed? So we can see, if we go into settings, and go down to the experimental features, so over here our upcoming features, you can see we've got delayed load.

[03:08] Speed up your app start time by setting on-demand screen expression calls. Now, do bear in mind that preview, which are beta or experimental, which are alpha, which mean they may change, break, disappear features. They would be ones which go away at any point but could they be used to improve your performance? You should also test your app in variety of different places.

[03:34] Like for instance, maybe you're over Wi-Fi, maybe you're using mobile phone data. Now, are there too many controls and screens? For example, could you use a gallery instead of using lots of different data cards, for instance? Can you have a menu in a gallery instead of having all of these different buttons? So imagine each of these buttons being an item in a table and you can click on them in a gallery.

[04:02] Maybe that can be done to improve responsiveness. Now, looking at the app itself, we have got an on start. On start runs once before the app can open. Now, this means that it's got to start and finish before you can use the app. So anything that happens in on start is blocking. Now, looking at a particular screen, you have got on visible.

[04:30] This runs every time the screen is open. So it could run lots of times. But it runs in the background. So it doesn't actually block you to use anything on visible. And of course, it will only happen when you go to that screen. So if you can delay something until you get to a screen, and then load information, for instance, then that might have a better performance. Now, there's something called delegation. And if I go back into the settings, you can see that we've got a data row limit.

[05:01] Set how many rows are retrieved from server-based connections where delegation is not supported. So what is this about? Well, suppose I wanted a table. Well, it might be a huge table, there might be tens of thousands of rows. So we let the server get those tens of thousands of rows and manipulate data on them. But there are some functions that can't be delegated, some functions which you do on your PC, on your iOS device, on your Android device instead of going up and down to the server.

[05:34] And where that happens, you don't retrieve the entirety of the table. Instead you just retrieve, in this case, 500 rows. You can adjust this all the way up to 2,000 rows but it will increase the amount of time for data to be processed because you're getting more data. So you can delegate functions onto the source. So we're talking about the Dataverse, SharePoint, SQL Server. These are very specific tabular data sources.

[06:03] So things that can be delegated include sorting. So sort or sort by columns can be delegated. Filter, search, lookup, these filterings can also be delegated. However, too many of them can be time consuming, of course. We've got aggregation, sum, average, min, max and we've also got add columns, drop columns, rename columns, and show columns. Those can also be delegated at the server, they can improve performance.

[06:33] Now, if you use a non-delegable function, you will know because you will see a yellow triangle saying this function cannot be delegated. So we're looking at string manipulation functions, left, mid, right. We're looking at signals, like for instance, a compass function or location function. They can't be delegated to the server because only your device knows where it is. We're looking at volatile functions, like RAND to get a random number.

[07:02] We're looking at collections but the good thing about collections, as you'll see in the next section is that can actually improve performance and also allow you to do things offline. And finally, you can have a look at the existing performance if you're using the Dataverse. So if I click on the gear here in make.powerapps.com, I can go to the Admin Centre, which is admin.powerplatform.microsoft.com, then we can go to analytics and we can go to Dataverse and within Dataverse, we can have a look at the entity usage.

[07:43] So in other words, what tables are being used when. So here you can see I'm using my expense table a lot. I'm using the account table a lot. We can change the filtered, so we can say well, I don't want it to just be for the last day but I want it to be for the last week or the last month or things like that. Now, we can also get analytics for your Power Apps as well. So again, in the Power Platform Admin Centre, admin.powerplatform.microsoft.com, you can get some detailed statistics.

[08:17] So you can get information about overall app usage, information about the location, errors by the end users. It can also help diagnose problem areas but you can see, we haven't add any problems with this particular Power App. So in this video, we've had a look at some of the implications of design choices on your app performance.

[08:46] Are you building too big an app? Could they be divided into smaller apps? What about network bandwidth and any latency issues? Are there too many controls and screens? Could you use galleries instead of data cards? What's the difference between the app on start and a screen on visible? And incidentally, if you're wondering what options I've got for the app, I can run the on start for the app whenever I want.

[09:16] And we've also had a look at settings and there may be some preview or experimental features that can speed up your app and you can also say how many rows can be retrieved from the server-based connection where delegation, we're going back to the server and saying you do the filtering, please, you're quicker is not possible. And we've also had a look at the app checker and we can see that it gives us information on Canvas app formulas, accessibility and performance.

[09:50] It gives us red circles for errors, yellow triangles for warnings and blue circles for suggestions.
