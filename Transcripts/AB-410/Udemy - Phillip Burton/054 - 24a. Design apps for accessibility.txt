# 24a. Design apps for accessibility
# Udemy — AB-410: Intelligent Applications Builder Associate exam prep — AB-410 — lecture 054 (section 12: Canvas apps: designing apps and creating reuseable components)
# https://www.udemy.com/course-dashboard-redirect/?course_id=4049210
# captions: English

[00:00] Hello. In the previous section we created this basic working Canvas app. Now there's a lot more that we can do. However, in this section we're going to turn towards the more specific requirements for the exam and having a look behind design principles and creating reusable components. In this video, we're going to look at how we can design and validate our Canvas app for accessibility. So first of all, what is accessibility? Well, it means that not all of the users will be able to use everything. For example, some people might not be able to use a mouse and will be relying on a keyboard for navigation. For example, if I go to this edit screen and play, then I can go between items by pressing tab and then pressing shift and tab to go backwards. But in what order are these controls arranged? But in what order are the tabs? Well, it should mirror visual order down or across. We're going to look in a later video at containers which can group related content together. Form card and gallery controls are automatically grouped. However, I can't press tab on everything. I'm not going to press tab and get the words claim description. So to control where the tabs stop, this uses a property called tab index. So it says zero for interactive controls, so ones where you can tab to, and minus one for non-interactive controls, where they cannot be accessed by keyboard navigation. Now you can manually control the navigation order by using numbers above zero, but it is not recommended as it could break screen readers. So now if I play this app and press tab, you'll notice that we go to expense, but the next tab goes to claim date and simply I can go back. So it is not going into this claim description because the tab index is minus one. So I will change it back to zero. Another way of controlling keyboard navigation is by using visible. So if I select something, visible, if it is false or if the display mode is disabled, will indicate that the control is not included in keyboard navigation. If I have a size of zero or one, or if I change the color to transparent, then it will not be visible, but it will be available for accessibility purposes. If I click on this label, we can see that there is a property called live. This determines how screen readers interpret changes to label content. So you might have the text of this label be defined by a formula, and if the results of the formula changes, then the value or the text will change.

[03:11] So we've got three settings for live. Live.off, no changes will be announced. Live.polite, it waits for the screen reader to finish before announcing the changes to the label content. And live.assertive, it interrupts to state the changes. So I will keep this as live.off. There is also a further property called role. So what role does this label have? Is it a heading one to four, or is it normal text, in which case we can use textrole.default.

[03:48] Now how well is your Canvas app doing for accessibility? Well, we can use the app checker to find out. So here you can see we've got accessibility. We can also use something called the solution checker, and we'll have a look at the solution checker in a later video. So first of all, you can see we've got missing accessible label. So this is a label for screen readers. So if I go to this icon, for example, we have accessible label as being a null string, blank. So you use accessible labels in image, icons, and shape controls to announce their presence to screen reader users. Otherwise, you will get this error, missing accessible label. We've got here an error, missing tab stop. So if you notice, these icons do not have a tab stop. So if I go into the properties, here we can see the tab index is minus one.

[04:47] So they will not be used if I press tab. So that's clearly a mistake. I need to change that to zero. So other accessibility problems that you may encounter, adequate color contrast. So is there sufficient color contrast between text and fill or background image for people to be able to see the text? Now you should consider what you call the screen. So screen readers will usually say the screen name when it is loaded, as it is usually the thing that is focused first of all. And we can use the function set focus immediately if you don't want the screen to be focused, but something else. If the screen has a default name like screen one, screen two, then you'll get the tip revise screen name. You may be adding picture buttons, but you must have text and change picture text for maximum accessibility for screen readers. Sometimes the button will get the focus and there is the focused border color and focused border thickness properties. If it is zero, then you will get the error focus isn't showing. Now let's add a new screen and insert into this screen and add picture control. So this is where you can upload a picture. So if you click on the picture, then you should have text and change picture text properties set for screen readers to use them. You should also set focus border color and focused border thickness for when it gets the focus when you click on it. If the focused border thickness is zero, then you'll get the error focus isn't showing. You can also insert audio and video controls.

[06:41] So if we look at the video control, if the closed captions, these are the subtitles URL is empty, then you'll get a warning missing captions. If the default controls are switched off, then you'll get an error missing helpful control settings and worse if auto start is switched on. So here you can see it starts immediately without me clicking on it. Then you will get an error turn off auto start. So if I go to the app checker and recheck all and going to accessibility, there we can see these warnings. Next, let's have a look at the pen control. So this is when you can use the mouse as a pen. Now if this exists, then you may get the error, add another input method. And if you want any information on any of these errors, just click on this arrow to get some information. And here you can see we've got the revised screen name error. Next, I'm going to insert a toggle. So this is an on off switch. If we look at the properties, we have got false text and true text. So I can switch this toggle on and off and you can see the text changes. So if I remove the text from true text and false text, I can see whether it is on and off, but somebody using a screen reader might not be able to. And so you would get an error, add state indication text. And then finally, if labels or markers for charts are not shown, then you may get the warning missing helpful control settings. And if a control contains HTML and the control is not a HTML text control, then you get a warning. HTML won't be accessible. HTML hypertext markup language is one of the main languages for creating web pages. So in this video, we've had a look at some ideas for how to design apps for accessibility. So if you want to validate your apps, then you can go to the app checker or the solution checker, which we'll have a look at in the next video.
