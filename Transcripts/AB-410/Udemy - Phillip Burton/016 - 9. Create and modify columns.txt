# 9. Create and modify columns
# Udemy — AB-410: Intelligent Applications Builder Associate exam prep — AB-410 — lecture 016 (section 3: Creating Expenses tables in the Dataverse)
# https://www.udemy.com/course-dashboard-redirect/?course_id=4049210
# captions: English

[00:00] In this video, we're going to expand our employee table by adding additional tables. So here I am in the table, and now if I go to new, it doesn't say do you want a new table, instead it says do you want a new column, or relationship, or key, or so forth. Now I can see what columns are included in our table by going to schema, columns. And though we have just added one column, the primary key column, you can see that if I go to system columns, that all of these additional columns have also been created.

[00:37] There is one called employee table, in other words, the exact name of the table, and this is the unique identifier, the GUID, which in database terms is truly the primary key. This needs to be unique, but this shouldn't be confused with the primary key that we have just set in the database. So let's add a new column, and this column is going to be a start date column. So I'll go to the primary name, so I will have the start date, and the data type, I'm going to use date and time.

[01:14] So we can have one which includes the time, or just the date only. So is it really necessary that we need to know what time an employee started their employment? And I'm going to say no it doesn't, so I'll just say date and time, date only. So the behavior is going to be simple, we'll have a look at calculated and roll up in a later section. And is it required? Well I'm going to say maybe business recommended, but not required. So what business recommended does is it adds a symbol in a model driven app, if the data is not there.

[01:52] Now you can enable this column to be used in advanced find, so do you want people searching for a start date? And I'm going to say no. You can also allow form fill assistance, so this means I've got a picture for example, of information that I want to add to a form in a model driven app. Do I want this column to be capable of being filled? And I'm going to say yes. There are advanced options, like for instance the internal schema name, whether it is time zone independent, I'm going to say yes, it doesn't matter what the time zone is.

[02:28] We may have different officers all around the world, but I want to say that if somebody was employed on the 16th of July 2028, then that was their start date, regardless of what time zone it was in. We can also enable column security, to stop all people from being able to read this column. We can enable auditing, so to log any changes or additions or deletions that have been made, but this will only be used if the table has auditing enabled.

[02:58] And there are a couple of settings as well for dashboards. So I'm going to click save, and if I then go back to the custom columns, and just wait a moment, maybe refresh, there we can see the start date. So let's have a look at some of the other data types. So we have got text, starting with plain text, so this is a single line of text, and this can use a max length, so maximum number of characters, and you can change the format, either in the format or in the data type.

[03:35] There is text area, so this is for longer text, still a single line, but it can be displayed over multiple lines, up to 4000 characters. So there is a multiple lines of text that can go up to around a million characters, but you can also limit how many characters will be used. There's rich text, this is formatted text. Email, so this is a validated email address, so you can't just enter any text into it, it needs to be an actual email address.

[04:05] A phone number, so this is a validated phone number. A ticker symbol, like on the New York Stock Exchange, and a validated website, and there are also multiple lines of text available as well if you want it. For number, we've got whole number, so numbers without any decimal point. Decimal, so you can use up to 10 decimal places, and it stores the number exactly. Float, this is an approximate number, up to 15 decimal places.

[04:39] So why might you use an approximate number type? Well it does give better performance, so that's to be used when the number roughly is sufficient and you want faster performance. A language code, a duration to the nearest minute, you can select from a list or type the number of minutes, or enter it as this many minutes, or hours, or days, or this number points something hours or days. And then a time zone. So for all of these, if I click on one, you can, and it didn't actually register, let's try that again, you can select a minimum and a maximum value, so you can't enter numbers outside of that values range.

[05:20] We've had a look at date and time. Lookup is used when you have a relationship between two columns, and it will often be auto-added by the relationship. Choice, now this has been previously called option set, so choice and yes or no gives you a range of values. And again it didn't work, so I'll just need to select that again. So I'm going to create a choice column called level.

[05:52] So let's scroll down and I can choose what sort of choice I want, including a yes or no Boolean. Boolean means on or off. So I'm going to create a new choice, and this choice is going to be called employee level, and the choices include staff, and manager, and senior manager. So I will click save.

[06:21] And then when you enter information about a new employee, it will mean that you can only select one of those values. So now I should be able to synchronize this with employee level. Now there's two different types of choices, a global choice that can be used in multiple tables, or a local choice that can just be used in this one table. So the question you need to ask yourself is, are you going to be using this choice in multiple tables? If not, you might want to consider a local choice.

[06:54] So the default choice I want when I enter a new member of staff is just staff. So let's save that, and then I will create another new column. So let's have a look at the other choices. So we have got, well in addition to choice you've got yes and no, which is a two choice scenario. So here you can see these two choices.

[07:22] We've got currency. So here you can choose a precision level. So not only do you have a minimum and a maximum value, but you can also say whether the number of decimal places is based on the currency, or I set it here, so maybe zero decimal places. And this is going to be for a salary, so I don't want anyone to have a negative salary, so I will start it at zero. You might want a significantly higher rate.

[07:50] And a maximum value, let's say, one followed by quite a number of zeros. So I will click save. Now if I go back into new column and have a look at the other data types. Now this will just take a bit longer because it is also saving a global choice as well. Then we have also got auto number. These are numbers generated by the server and it's really a text column.

[08:21] So what you can do is have an auto number with a string prefix. So you might have employee and then have five digits starting at a thousand, for example. Or you might have a date prefix. Or you might have a custom prefix. So those get a bit more complicated. Then we can have files or images. You can have a maximum of one image column per table and the column is always called table image.

[08:53] And then there are formulas and prompts and we will be looking at those in later sections. So I'm just going to add one additional column and that is going to be expense approval limit. And again, this is going to be a currency. And click save. You notice that when you create a currency column that there are two columns created because the currency might vary and you might have a local currency and your organization's currency.

[09:27] So in this video, we've seen how we can create and modify columns. So if I go back to the list of tables and click on my employee table, then I can go to new column or I can go to schema columns and go to new column. You can then enter the display name, a description, and very importantly, a data type. And then once you've done so, you can, to some extent, modify these columns. So if I click on a particular column, there are some things you can't modify, for example, the data type, but you may be able to modify other things.

[10:05] For example, if I go into this choice column, then you could modify what choice is being used and the default choice. You might also be able to modify the format that is used.
