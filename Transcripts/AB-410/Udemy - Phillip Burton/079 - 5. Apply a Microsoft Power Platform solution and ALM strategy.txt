# 5. Apply a Microsoft Power Platform solution and ALM strategy
# Udemy — AB-410: Intelligent Applications Builder Associate exam prep — AB-410 — lecture 079 (section 15: Design Microsoft Power Platform solutions by using AI-enabled tools)
# https://www.udemy.com/course-dashboard-redirect/?course_id=4049210
# captions: English

[00:00] In this video we're going to have a look at an ALM strategy, Application Lifecycle Management. So why do you need an ALM strategy? Well let's say you have got a canvas app and you make a change and save it, but unfortunately something bad happens with that change which means that thousands of users who are using that app suddenly can't use that app properly. It would be much better if you had separate environments, one for development, one for testing and one for production and then you can make your changes in the development environment and then test it, then move them into the testing environment where it could get more wider testing before finally going into the production environment. So I'm going to create a new environment. So I'm going to go to settings or wheel at the top right, go into admin center. This takes me into the power platform admin center or PPAC and I'm going to go to manage, new and create another production environment called ab-410 test and change the default settings. I will add a dataverse data store. I will add a security group, in this case open and I will click save. So now let's go back to the power apps portal while that's happening and go into solutions. So there are two different types of solutions, unmanaged solutions and managed. Unmanaged solutions are used in development environments and as we know there are two different default unmanaged solutions. These unmanaged solutions use the default publisher and the publisher prefix assigned to the solution. Now in this course we have created a dev solution and if I click on it and go to objects here you can see all of the objects that we have created during this course. Now what happens if I was to delete this unmanaged solution? Well it is only the unmanaged solution container which is being deleted. All unmanaged customizations then belong to the default solution. So as I say unmanaged solutions are for development environments.

[02:22] If you want to deploy to a production environment, a testing environment, a user acceptance testing environment or UAT, a system integration testing or SIT environment, then you probably want a managed solution. So managed or in other environments called controlled solutions should be generated by exporting an unmanaged solution as managed. In a managed solution components cannot be directly edited. You can add them to an unmanaged solution but that would create a dependency. Managed solutions with a dependency cannot be uninstalled. So you export an unmanaged solution to a managed solution. You can't export a managed solution, you can only delete it. And if you delete a managed solution then all components as well as the solution will also be deleted. So imagine the managed solution as being a sort of blacked as a box that is a whole. You can get rid of the box but you can't alter the box significantly. You might be able to do some customizations but that's about it. So we have created a new solution. If I go back into environments there it is. If I click refresh it has been successfully created.

[03:42] And if I go into a solution we are able, if I go into objects, to create new objects and also to add existing objects from other solutions. So let's export a solution and of course we can only export an unmanaged solution. So I can check my solution. You cannot export the default solution. And then we can go to export solution. If you want to make changes that you wish to export then click on publish. So this is the same as clicking on publish all customizations.

[04:20] Now you can see that there is an alternative to this export import process. This is called a deployment pipeline. We'll look at that very shortly. So if I click next you can see that there are some missing unmanaged components. So I'm going to add those to my solution. So now I'll click next and then it's asking me do I want a managed solution or another unmanaged solution. It also asks what the version number is going to be. So there are four numbers separated by dots. These are the major number, the minor number, the build number and the revision number. So you can see this would go from 1.0.0.0 to 1.0.0.1. And I can also run the solution checker on export as well to see if there are any errors. So I will click export and this will take a bit of a while. So this will take a few moments. Now while it is exporting I will go into the solution and on the left hand side go into pipelines. A deployment pipeline is another more automated way to transport apps and components from one environment to another.

[05:36] It contains sitemaps, tables, processors, web resources, floors and more but not data. If you want to move data from one environment to another you need to export it in the dataverse and then re-import it. So I can create a new pipeline. So this could be the AB-410 pipeline and the target environment would be the AB-410 test that I've just set up. And I'm going to click save. So this is creating the pipeline. Now notice that target environments in pipelines must be enabled as managed environments. So now you can see I can with the press of a button deploy here have one process which exports a solution from one environment and imports it into another. So I will click next and you can see it is now validating what I'm doing.

[06:38] Loading connections and you can see that it will re-establish connections to activate my solution and click next. It's going to give me the next version number 1.0.0.2 and it has created deployment nodes and now I can click on deploy. So in this video we've had a look at an ALM strategy. So we can use pipelines to export from one solution.

[07:14] From one environment to another or alternatively I can click on a solution and go to export solution and then when it has been exported it will be a zip file and then I can go to a different environment and click on import solution. And if for some reason your import was not successful then there will be a hyperlink download log file. It will probably show that some required components were not included in the solution that you exported.
