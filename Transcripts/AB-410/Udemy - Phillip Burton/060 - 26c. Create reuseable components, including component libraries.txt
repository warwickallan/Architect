# 26c. Create reuseable components, including component libraries
# Udemy — AB-410: Intelligent Applications Builder Associate exam prep — AB-410 — lecture 060 (section 12: Canvas apps: designing apps and creating reuseable components)
# https://www.udemy.com/course-dashboard-redirect/?course_id=4049210
# captions: English

[00:00] In this video we're going to have a look at another reusable component, the component library. So a component library contains components and you can create components in the tree view by going to component and new component. So what is a component? Well all of these are components. So these all have properties. So if I click on one you can see you can adjust things like the text, font, font size and weight. You can also use formulas with components. So you could create a new component by clicking on new component and here you can see a screen. So a component only has the one screen and you can adjust the size and then insert other components into it. And it also has properties, both input and output properties.

[00:58] However this is really only useful if you are creating a component for this one app. Yes there are import and export component buttons. However if we go to settings, updates and retired you can see that the export components and import components operations are deprecated and you should use component libraries instead. So at some point you will no longer be able to import and export components. So I'm going to delete this component and create a component library. So I go back into the PowerApps portal make.powerapps.com and it doesn't seem immediately obvious where to create a component library. What you need to do is click on more and then discover all at the bottom and then scroll down and we've got component libraries. So if I click on this now I can create a component library. I could also click on new component library as well. So I'm going to call this my component library and click create. So now this looks like just any other canvas app but you are specifically there to create components. So here we can see we have a component already added and what I'm going to do is create a total cost calculator. So what I need is an input label.

[02:33] So this will have cost before tax, another label, sales tax percentage and a final one, cost after tax.

[03:03] And now let's insert text inputs. So this first one is going to be called text cost before tax and the default is going to be 100. So next text input. So this is going to be text sales tax percentage and the default for this is going to be five as in five percent and I don't then want another text input for the cost after tax.

[03:46] This is the end result of the calculation so I want a text label instead. So this will be the text cost before tax dot text because by itself this is a control and I actually want the text. So I need to say dot text multiplied by one plus text sales tax percentage dot text divided by 100. So we get the answer 105. So I'm just going to reduce the height maybe to 320.

[04:30] There we go. So let's rename this component. I'm going to call this cost after tax calculator. So now let's save this component library. Just takes a few seconds. Wait till that circle disappears. There we go. It is now saved. So now I'm going to go back into my expense app and I'm going to insert a component but I can't see my new component here. So what I need to do is click on get more components. So you can see it represented as a folder and here you can see the cost after tax calculator. So I will import that. Let's add a new screen. So here we've got this screen one so I will add a new screen, screen two. And I will now insert, go into library components, the cost after tax calculator. And now I can use it. So if I play I can change the cost before tax and the sales tax percentage. And you can see I get an instant calculation. However at the moment this is just a black box. I can't feed anything into it or get information out of it. So I'm just going to save this power app. So do I want it to publish the version? Yes. Let's say yes I do.

[06:03] So I will click on publish and publish this version. So now our app can be made available to others. So in this video we created a component library and we created a component which we are using in this app. And of course I could use it in many other apps as well. I just need to create it once and then I can use it multiple times. But what if I need to update this component? We'll have a look at what happens in the next video.
