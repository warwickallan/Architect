# 28. Implement error handling
# Udemy — AB-410: Intelligent Applications Builder Associate exam prep — AB-410 — lecture 065 (section 13: Create canvas apps)
# https://www.udemy.com/course-dashboard-redirect/?course_id=4049210
# captions: English

[00:00] In this video we're going to have a look at some ways in which you can implement error handling. So suppose you have a syntactical error, so for example I put just one L in collect. You can see that syntactical errors are highlighted when you write the error, and of course they should be corrected as soon as possible. So here we can see the red underline, we can see various x's which depends on this collection, and we can also see it in the app checker. So if I click on formulas you can see that we've got multiple problems all stemming from the same cause. So if I correct this formula now you can see there are no errors found in the app checker. But it doesn't need to be a syntactical error.

[00:44] Let's go back to our component. So we have got a component here where we've got the cost before tax, so we could say 200, and the sales tax percentage, so I could say 10. And that gives me a cost after tax of 220. But what instead? I said that the sales tax percentage was 10, written out like this. Now you can see the problem, the value 10 cannot be converted to a number, and we have a red x repeating that error message. So what we can use are one of three different functions, is blank, is error, and if error. Is blank allows you to check if the user input is empty. It returns true or false. Is error returns true if the argument results in an error.

[01:37] So what I could do is say if is error, and here's my calculation, if this is an error, then give me a blank. Otherwise give me another value, and you can see that the end result is blank. That's because this currently is an error, and therefore returns a blank. If it wasn't an error, change this back to the number 10, then this is not an error, therefore we go into the false section. We can also use not is error, but if error. So if error looks at the first argument, if it is not an error, then it gives me that. Otherwise we move on to the second argument, and then the third argument, and so on. So now we've got is this first argument an error? If not, give me that. If not, give me blank. If, however, the second argument also resulted in an error, then we'll go on to the third argument, and the fourth argument, and so on. So if I change this back to the word 10, then we've got an error in this first argument, so we go on to the second argument. So I will change this back to a 5, and this back to 100. Now if we go back to our app, and the tree view, and the edit screen, and I'll just change the length of this rectangle so we can see the icons. We've got this check mark. So what happens when I click on it, is that it then invokes the on select property, which has this submit form function. Now once the form has been submitted, if I go into the form, it then runs the on success property. So in this case, it runs the formula back. However, what happens if the submit form is not a success? Well this is when you can put something into the on failure property. So we could use the notify function. So I can give an error message, there was a problem saving this form, and have a notification type. So an error, information, success, and warning. So I will say notification type dot error, and close the brackets. And finally, if there's a more generic error, then we can go into the app on error property, and then we can code something there as well. So perhaps another notify, there has been an error somewhere in this app. So in this video, we've had a look at how we can implement error handling. So syntactical errors should be corrected as soon as possible. However, there might be more errors based on the inputs that are received by certain functions, and therefore you should either check those inputs, or check the output, and give an alternative value before it results in an actual error. So you can catch the error and give an alternative result.
