# 6, 8, 9, 10. Create and edit tables in the data workspace
# Udemy — AB-410: Intelligent Applications Builder Associate exam prep — AB-410 — lecture 013 (section 3: Creating Expenses tables in the Dataverse)
# https://www.udemy.com/course-dashboard-redirect/?course_id=4049210
# captions: English

[00:00] Hello and in this section we're going to turn to the Dataverse and creating tables. So what we're going to be doing over the course of quite a few sections is creating an app where we can submit expenses and eventually have them approved. So in this video we're going to have a look at one way that we can create tables and this is in the data workspace. So if I go to tables on the left hand side we can see at the top that there is new table and create new tables. There is also a get started with CoPilot. So if I click on create new tables then we have this start with CoPilot option. So if I click on this then we can describe what business problem we want to solve. So I want employees to be able to submit expenses.

[01:08] Now there is a table options button and that allows me to change what is going to be created. Do you just want one table or multiple tables? Do you want a small medium or large table size? And do you want to include relationships? I'm just going to leave this as is. Now you can make this a lot more descriptive you can have up to 2,000 characters but I'm just going to click on generate and we'll see what CoPilot does. So this will just take a few moments and here you can see what CoPilot has created. We have got two tables with a relationship between them and various columns. So let's have a look at the data workspace. So in the command bar we can go back to leave the data workspace. We can create a new table so we can either do this using CoPilot or we can add columns and data to a blank table or we can start with an Excel or CSV comma separated values file or a SharePoint list. I can also add an existing table from the dataverse. So here we can see all of my tables. We can narrow it down by clicking on only in this solution and we can also view or hide data. So first of all I would need to select a table and then I can view the sample data that is contained within that table. So I'm just going to move this up and I can zoom in and zoom out. I can also fit to view and I can also have a map because this is useful if you've got lots of tables and want an overall view. Now we're going to have a look at relationships in the next section but you can see that this ties together two tables. So if I click on it I can edit the relationship options. You can see that here and I can also create a new relationship by clicking on create relationships and I can also drag between one table and another to create a relationship. So if I click on create relationships you can see that we've got two different relationship types one to many and many to one. So what's the difference between the two? Well we've got employee and expenses. Can one employee make many expenses or can many employees make just the one expense? Well generally a single employee would submit an expense claim and then could create another expense claim. So this would be one employee with many expense claims and we can see that next to the dashed line. We've got one and a star. Star meaning many. So we can then select a table. So employee and expense table for example. We can create a lookup column. We can say whether a value is required in the relationship. So for example can expense claims be submitted without actually needing an employee to submit them. So I'm going to say that yes you would need an employee to submit an expense claim and we've also got some internal namings that we can do as well. Now if I click on the dot dot dot next to a table we can view or hide the data. We can have a look at row ownership. So when I enter a rows data is it visible to me or to a team or is it visible across the entire organization. Going back to the view options I can have a look at properties. So I can change at the table display name and the plural name. So one employee, multiple employees. I can add a description. I can define the primary column. This would be a text column less than 850 characters which identifies what this row actually is. So quite often you would have an auto number which is used to identify this row. And if I click on advanced options you can also edit the internal schema name for the table. Going back to the view options you can also remove the table and data. So if I look at the data for the expense table and it took a few seconds for view data to become visible here. Then we can add new rows. We can add new columns. We can change the row ownership as before. We can edit the table properties as before and we can also fill the data to occupy the entirety of the window. We can also click on a column to edit the column. So we can edit the display name, the data type. We'll have a look at data types later on in this section but there are a few data types at the time of recording that you cannot use in the data workspace. So these are called rich text, customer, auto number or formula data types. So we'll have a look at auto number and formula data types again in later videos. So if you wanted any of those you would have to create those columns outside of the data workspace.

[06:29] We've also got the format, whether it is required and there will be a red star or asterisk next to the column if it is required. And we can also edit its internal schema name. We can also insert new columns and apart from the primary key we can also delete columns. Finally you can also use the copilot pane and if you can't see it then click on this icon to alter the tables. So you can enter a prompt and click the arrows or you can go to view prompts and have suggestions for create a table or tables, rows, columns or relationships, import Excel or comma separated values, CSV file or SharePoint list, rename the selected table or column, change a column or relationship type, delete a table column or relationship or suggest a table column or relationship. So let's ask copilot for some tables that it might want to suggest that we create and in just a few seconds here we can see we've got some other types of tables. So let's say I wanted an expense category table it can then create one and then once you're finished creating all of your tables then you can click on save and exit. So I'm not going to use this new expense category table so I will remove it. I will click on save and exit, save and exit and this will just take a few moments. And here you can see our employee and our expense tables. So in this video we've seen how we can create tables in the data workspace. Now I'm not actually going to be using these tables for our project so I will click on the dot dot dot next to them and then go to delete.

[08:35] Now there are multiple ways to enter the data workspace. So first of all we have got get started with copilot and start with a blank table that you can also access by going to new table, create new tables. And then if I go back and I will delete this expense table. Another way is by going to create on the left hand side and then we can start from data and create new data. And then that takes me into the data workspace where we can then start creating our tables, columns and relationships.
