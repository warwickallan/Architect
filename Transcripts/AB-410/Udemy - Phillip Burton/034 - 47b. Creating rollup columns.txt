# 47b. Creating rollup columns
# Udemy — AB-410: Intelligent Applications Builder Associate exam prep — AB-410 — lecture 034 (section 6: Building relationships and calculated, formula and rollup columns)
# https://www.udemy.com/course-dashboard-redirect/?course_id=4049210
# captions: English

[00:00] In this video we're going to have a look at how we can create a roll up column. So at the moment we've got these various expenses and we have got these employees and we have a subgrid showing all of their expenses. Now suppose I wanted a column which shows the total amount. So this is the total amount after sales tax. We can do this using a roll up column. So this can calculate from other tables. So if I go back into the tables and go into my employee table and create a new column. So this would be expenses claimed. I can change the data type to whatever I want. So I will make it currency and the behavior I will change to roll up. So now I will save the column and then edit the roll up calculation. You can have up to 10 roll up columns per table and 100 per organization. So here you can see we're back in the classic interface and I need to add a related entity. So entity being the old word for table.

[01:14] So I will add the expenses table and then you can filter it so you don't have to add up all the rows in a table. So let's say I wanted a condition where the claim status is equal to draft and let's check that. So now we've got the aggregation, the calculation. So I want the aggregate function to be sum. You can use max, min and average. You may also be able to use count. So I want the sum of the total amount and I will check that. Now notice what it says at the bottom.

[01:53] This mass calculation job will automatically be scheduled to run approximately 12 hours from the time that the roll up field is saved. So it is not an immediate calculation and you can see the default is 12 hours but your system administrator can change it so that it can be calculated every hour. So I will save and close this. So this is the expenses claim. So I will rename it as the draft expenses claim. So here we can see expenses claimed. So I will edit that and call it draft expenses claimed. Now notice that it has created additional columns as well. So we've got when it was last updated on, we have the base because it is a currency data type and we also have the state. So now let's have a look at our data. So here we can see the expenses claim draft is completely blank and if I go down we can add the expenses claimed state and this is currently blank as well. The state column will contain the current state of the roll up calculation.

[03:06] So it could be not calculated, calculated. Overflow error, that would be too big a calculation for the data type. Other area, that's a temporary internal error so next time it is recalculated it will probably calculate correctly unless there's another problem. Retry limit exceeded, this is due to concurrency and locking conflicts. Hierarchical recursion limit reached, so you might have a hierarchy. So that is when an employee has a manager, has a manager, has a manager and so on.

[03:37] So when that limit has been reached and there could be a loop detected. So for example one calculation is based on another calculation which is based on the first calculation. So now let's go into the employee forms and I will add this new column, the expenses claimed draft into my form. So there it is but I will drag it elsewhere and you can see last updated not available so that is the date column. So I will save and publish this form and then I will go back and update the view.

[04:21] So now let's update the view and I will add in the expenses claimed draft. So let's save and publish this. So now I'll go into solutions and click on publish all customizations just in case and now let's go back into our app. And let's go into the employees view and there we can see expenses claimed as a draft and there we can also see that column as well. So now when I play I can't see this new column so let's just refresh this page, maybe ctrl and click refresh. So if I go into a form there we can see it and if I go back there we can see it in the view. So it can take 12 hours for it to calculate so I'm going to pause the video now and come back to it tomorrow. So it's now the next day and you can see we now have a figure in the expenses claimed. So if I click on it here we can see the figure as well. So while recording the videos yesterday I changed the claim status of some of these items so you can see there's only one item which is draft which is 1100 pounds plus 220 pounds equals 1320 pounds which we can see on the view as well. So in this video we've seen how we can create a roll-up column. So we go into tables, select a table and go to new column and select our data type and then for behavior choose roll-up. So this is useful for calculating totals count and earliest and latest date and time.
