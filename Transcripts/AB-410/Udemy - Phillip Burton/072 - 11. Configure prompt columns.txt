# 11. Configure prompt columns
# Udemy — AB-410: Intelligent Applications Builder Associate exam prep — AB-410 — lecture 072 (section 14: Create prompts and models in AI Hub)
# https://www.udemy.com/course-dashboard-redirect/?course_id=4049210
# captions: English

[00:00] In the previous videos, we consumed a prompt in a canvas app and in a Power Automate cloud flow, but we didn't consume it within a model-driven app. Now there are various ways to start, for example, Power Automate flows from data being entered into a model-driven app. However, a model-driven app is powered by a Dataverse table and we can insert a prompt column into a Dataverse table so then the prompt will be calculated based on new data. The results from the prompt will be stored in that prompt column and then you can use it just like any other data.

[00:38] Use cases include answering customer questions. When customer questions are stored in the Dataverse tables, replies to those questions can be created. Content creation. It can help with creating blogs, reports, or marketing text. Data analysis. You can create analyses, evaluate insights, or classify data. And workflow automation. You can schedule, report, and enter data automatically. Prompt columns are licensed in the same way as AI Hub prompts and you can have up to five prompt columns per table. So let's go into the expense table and create a prompt column.

[01:16] So I will go to new column and I'm going to call this the category column and the data type is going to be prompt. So the format is text and you can uncheck allow form fill assistance because I don't want this prompt to be used when you are filling in data and you just give it, for example, a document and it tries to assign which columns I want parts of the document to go to. And if you don't want the prompt to be active then you can deselect allow prompt column execution. So now I'm going to add a new prompt and you can see here the default prompt classified the expense into one of the following categories and it gives politics, business, sport, entertainment, logistics, health, news, art, or unclear. So that's probably not a good list for expense categories. So instead let's change these to travel, meals, accommodation, office supplies, IT, or unclear, books or magazines, or unclear.

[02:36] And you can see the rest of the instructions. Now we have got this text input. This is the record id that is needed for the prompt column and if I go to this expense column you can see that it is being filtered on that text value. In other words this will just be for the current row but for that to happen you need to keep these two in the prompt. So you can see here my prompt name. So I'll just trim it down a bit. Now testing doesn't work that well because you don't have the prompt column record id and so it will probably say I don't know or maybe it will just go to unclear. And you can see for me it actually timed out. Now I can change the model and here you can see various different types of models some of which would need to be enabled by your administrator and you can also add Microsoft Foundry models as well. So those can include custom or fine-tuned models and if I click on the dot dot dot you can also clear the prompt and go to settings where you can change the temperature, record retrieval, add links in the response, enable code interpreter, and change the content moderation level. So now let's save this prompt within this column and now we can save the column. But notice what it says at the top. Prompt column values are generated asynchronously. In other words they will not immediately appear. They will be calculated at some point, probably some point soon.

[04:14] So let's click save. However one thing they will not be calculated for is existing data. So they will only be recalculated if a column on which the prompt relies on changes. So if I refresh this page and then in the expense columns and data and if I scroll to the right we can see that there is a new column called category. But that's not the only new column which has been created. We also have two new columns ending prompt column status. This will either show zero, not started, one in progress, one in progress, two completed, or three failed. And you can see it's all blanks because it's not actually going to create a category for existing roles. And there is also another column ending prompt column details. And again at the moment this will be blank as well. Let's add another column. So I'll go to new column and this column is going to be approval recommendation.

[05:29] So based on the information that has been provided does the AI think that this prompt should be approved. So let's add a new prompt and I'm going to keep in these two items here. So prompts should have clear instructions, have sufficient detail and be placed in sufficient context. So I will say based on the expense details below recommend whether this expense should be approved. So the description is and I'll put a slash and go to expenses and claim description, amount, and I will put in the total amount which is a calculated column.

[06:20] And you might want to put in additional columns as well. So respond with approve, review, reject, unknown, and provide a brief justification. So I will rename this prompt expense approval recommendation and I will save it. And then I will save the column. So I will uncheck allow form fill assistance for this column as well and click save. So now that this column has been saved I'm going to go, so let's do this again. So now these columns have been added I'm going to go into my form, my main form. So I will edit it and I will add into that form the category and the approval recommendation. So I'll just change the label so there is a space in there. And then I'll go to save and publish. So after that I will go back. I don't need it in the view but I can add it if I wish to as well. So once it has finished saving and publishing I will go back, go to views, my active expenses default public view.

[07:55] And I will add these two columns in the view as well and save and publish and then go back. So now I'll go back into solutions and publish all customizations which will just take a moment. And now that has been done I will go into apps and play my employee and expense model driven app. And of course you can also insert these columns into a canvas app gallery or form as well. So I will go into my expenses. So there we can see the two new columns. So I'm going to add a new column.

[08:33] So this expense is for three technical books about the Microsoft A B exams. And maybe I will copy and paste that into the claim description, give it a claim date, an amount before tax, amount of sales tax and add the employee and now click save. And because these prompt columns are created asynchronously we have currently got on view blank category and approval recommendation columns. However if I refresh this page now we can see the category is book stroke magazine and the approval recommendation is approve. So it is fine with the purpose of the expense and also the total amount. And if I go back we can now see that the category and approval recommendation has been filled in for this row in the view. So in this video we've had a look at how we can create and configure prompt columns.

[09:45] So we go to a relevant table and then go to new column. Use the prompt data type and then we can add a new prompt.
