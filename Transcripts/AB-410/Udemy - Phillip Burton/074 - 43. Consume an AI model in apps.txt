# 43. Consume an AI model in apps
# Udemy — AB-410: Intelligent Applications Builder Associate exam prep — AB-410 — lecture 074 (section 14: Create prompts and models in AI Hub)
# https://www.udemy.com/course-dashboard-redirect/?course_id=4049210
# captions: English

[00:00] In this video we're going to see how we can consume an AI model in a canvas app. So one way is by going to the AI hub and AI models. I will click on all, find a model you want to use and then click on use pre-built model if it's a pre-built model and use in a flow or use in an app. If it's a custom model you want to use then you would need to train it instead. So if I click on use in an app you can see that a new canvas app is going to be created and this will just take a few seconds.

[00:40] And here you can see a sample app. So we have got a text input then we've got an analyze button that again sets a variable to the model name dot predict and in brackets the text input and then we've got dot document dot top sentiment. And if I expand this you can see we've also got an if error as well. So if there's an error with this text input sentiment then use the notify to let me know. Now that sets this top sentiment into a variable called text input sentiment and then you can use that variable dot name and then while I'm having problems clicking on the confidence score so I will do it over here we have got dot confidence. So if I test this so I play and click analyze then we have got the sentiment and the confidence score. Now what if I wanted to use an AI model in an existing canvas app? Well then I would go over to data and I would add data AI models and select the model for example the entity extraction model. So if I select all of these and delete this and the name of the text input box is called text input one I can insert a text label and I can say entity extraction dot predict and then I want text input one dot text and then press a dot and see what properties you've got. So I've got here for example entities as a property. So how I can put that into a single text box or label is by using the concat function and then saying for each entity I want and here you can see we've got type value confidence length start index and this record. So I'm going to use type and and add to each type a comma and a space. So if I run this now and say something like the first thing to do in London is to go to Buckingham Palace.

[03:18] Then we have got a city and an organization as the type and we could also extract the name and so on. Now you notice that the computer was continually retrieving the sentiment and of course that will consume multiple credits. So a better idea is to use something like this button to set the variable but only when somebody clicks on it. So that means they can type text in the text analyze and then only be charged one set of credits and not multiple. So there are multiple different types of AI models that you can use. For example the text recognition model.

[04:00] So these have these properties original image and results dot and selected dot bounding box these are the coordinates page number and text. For receipt processing you have these properties and these are all strings merchant name address and form transaction date and time purchased items dot name price quantity and total price subtotal tax tip and total. So you've got these two different types of AI models ones which produce a table and then you can use the concat function to obtain all of the types and you've also got ones that don't produce a table and so here you can see I'm just using model dot predict dot and in this case document dot top sentiment. So with category analysis you've got dot categories with language detection you've got dot language with key phrase extraction you've got dot phrases and with object detection and document detection you've got properties such as model id original image you'll have fields and tables in document detection and you would have some sort of results or group results which contain things like bounding box confidence page number and value. So generally there will be a what I'm extracting and the confidence that I am correct. Now there is one other sort of AI models that you can use in a canvas app so if I create a new screen and these are ones which already are built in to the insert menu of the canvas app and we can see we've got a business card reader receipt and form processor object detector and text recognizer. So if I use a business card reader and here you can see I can scan a business card so I will click play click on scan business card and here we've got a business card which you can download in the resources near the beginning of this course click open and then this business card will be analyzed this will just take a few seconds and from here I can then insert a text label for example and this business card is called business card reader one so I can say business card reader one dot and then just have any of the properties for example business form so you don't have to use a predict function with any of these components it is already built in but you can if you wish to go back to the data pane and add in a data model for a business card reader it's your choice what you would like to do.

[06:55] So in this video we've seen how we can consume an AI model in a canvas app so you can go to the AI builder and click on use pre-built model and use in an app. Alternatively you can use one of the built-in AI builder components or go to data on the left hand side and add data and go to AI models.
