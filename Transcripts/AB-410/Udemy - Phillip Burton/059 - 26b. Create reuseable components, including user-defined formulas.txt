# 26b. Create reuseable components, including user-defined formulas
# Udemy — AB-410: Intelligent Applications Builder Associate exam prep — AB-410 — lecture 059 (section 12: Canvas apps: designing apps and creating reuseable components)
# https://www.udemy.com/course-dashboard-redirect/?course_id=4049210
# captions: English

[00:00] Now in addition to creating named formulas in the app.formulas property, you can also create user-defined functions. So let's say I wanted a function that takes two arguments, a price before tax and the sales tax percentage, and then gives you the price after tax. So let's create this user-defined function. So once again I go into app formulas. So I can say my function name, so this is going to be price after tax, and I need to define my parameters, my arguments. So I want the price before tax as the first argument, and what sort of argument is it? So I say colon, and then I can give it a data type.

[00:51] So it could be boolean, true or false. It could be a choice or yes, no. It could be a color. It could be a currency in a floating point number. It could be a date, date time or time. It could be a decimal, float or number. It might be dynamic, so that would be a variable data type. It could be a GUID, globally unique identifier. Could be a hyperlink. It might be an image or media. In other words it would be a URI or URL text string. It could be a record which contains a row or a reference to a record. It could be a table containing multiple rows. It might just be text, or it could be void where there'd be no return type. Now you might be asking, why would I create a user-defined function that doesn't return anything? Well don't forget these formulas don't just have to calculate an answer, they can do actions. We have used formulas such as navigate and back. You could also use other formulas to add data into Dataverse or other data sources for example. It doesn't just have to do calculations, which is what I'll be doing in this user-defined function. So I'm going to say that this is a number. So let's have the sales tax percentage as a second parameter, and this too is going to be a number. And then we have to give it an output type. So this is going to output a number. So you can see the difference between a user-defined function and a named formula. So we've got these parameters, and we've also got the input and output data types. So this is going to be equal to the price before tax multiplied by, and in brackets or parentheses, one plus the sales tax percentage, and maybe divided by a hundred. It depends if you give this formula five or five percent, which is 0.05. So now let's use this user-defined function.

[03:04] So let's insert a text input. So this is going to be the price before tax. So I will have the default as a hundred. And let's insert another text input. So I'm going to say that this is going to be five, as in five percent. And let's rename this. So this is going to be text sales tax percentage, and make sure you press enter, because if you don't, then if I show you on this one, I just type something and then move away, that what I've renamed it to will not be saved. And this is going to be text price before tax, and press enter. So the output is going to be in this text label, and it's going to be using the function price after tax. And I need the price before tax, so that is in the text price before tax, but that is not a value. If I have a look at this, this is a control. I need to have the text of it, so I need to say dot text. And then I want the text sales tax percentage, and again dot text. And let's close that. And there we can see the final figure, 105, which is 100 plus five percent tax. So if I play this, I can change this, so we have 200 pounds.

[04:39] And notice that instantly we have a different answer. So if I apply 20 sales tax, you can see the final figure is 240 pounds. So in this video, we've had a look at the reusable component of user-defined functions. So again, you can define it in app dot formulas, but in this case, we can provide in brackets or parentheses the parameters we want to use as input together with the type that we've got, in this case, colon number. And we also need to define the output data type, colon number, and then an equal sign, and then our function, which might be mathematical, it might manipulate figures, or it might include actions such as set to change the results of variables, patch to add rows to data sources, or it could be navigate or other actions.
